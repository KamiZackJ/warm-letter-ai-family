# 暖笺 1.0.0 正式发布记录

本记录区分代码完成、部署、上传、审核、发布，避免把体验版称为线上正式版。

## 23:30 候选包更新

- 2026-09-23 23:30:46（北京时间）重新上传 1.0.0，包体 992,283 字节，微信版本管理已显示新时间及“修复跨设备家书和照片读取”的备注。该包替代下文 21:19 的旧候选，仍不是已提审或正式线上版本。
- 对应修复提交 `dd022b4` 已推送 GitHub。小程序全量 31 个测试文件、260 项测试通过，类型检查通过。
- 首页打开本人已确认家书改为所有者预览；新设备缺少分享凭据时可恢复入口，并明确提示此前分享失效，需重新发送。有效本地凭据不重复补发。
- 本人照片通过固定正式 API、Bearer 鉴权恢复，包含失败重试、12 秒超时、离页取消、迟到临时文件及账号删除后的清理；未开放素材的匿名访问。
- 模拟器实际验证首页、隐私页、本人已确认家书预览及分享入口；数据管理页首次网络超时，后续重入成功。照片恢复的本轮证据为自动化测试，手机端完整闭环仍待用户执行。测试中缺失凭据的旧测试分享已被补发操作撤销。
- 代码审核表单已打开并填写真实功能说明，尚未最终提交。照片/录音微信审核仍为 `failed/-1008`，不得以表单准备或候选上传代替审核成功。

## 9 月 24 日 00:05 下载对照

修复提交及更新记录的 CI 均成功，最新记录为 [2a17383 的 CI](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/35882408685)。同一测试账号已刷新微信登录，并完成以下独立素材审核对照：

| 对照 | 微信实际结果 | 提交至回调 |
| --- | --- | --- |
| 阿里云官方公开 JPEG，HTTPS | pass | 约 3.575 秒 |
| 当前 API 主机公开合成 PNG，HTTPS、无查询参数 | failed / -1008 | 约 3.749 秒 |
| 当前 API 主机同字节 PNG，临时 HTTP、无重定向 | pass | 约 4.861 秒 |

结果支持继续定位 HTTPS 下载链路，尚不能单独判定某个 CA 或 TLS 算法有错。临时静态路径和隔离诊断记录已全部撤销，所有真实素材仍要求 HTTPS 和自身的微信审核通过。准备一次可信免费 CA 独立预签发对照，未取得或启用新证书；原有效证书继续服务。隐私指引后台仍为“审核中”，代码未最终提审、正式版未发布。详见[内容安全对照证据](./WECHAT_CONTENT_SAFETY.md)。

## 9 月 24 日 00:33 证书链对照

- 已独立取得并验证 ZeroSSL RSA DV SSL CA 2 签发的 90 天证书，以短时手动载入方式对照。实际新连接在 SNI／无 SNI、TLS 1.2／1.3 下均返回新指纹，系统信任链与域名验证通过。
- 00:32:52–53 重新提交现有真实照片和录音的微信检查，00:32:57 两项回调仍为 `failed/-1008`。不同 CA 并未解决微信下载问题，不能宣称证书切换修复成功。
- 对照后已恢复原 Let's Encrypt 配置、证书及自动管理；API 和数据库未重启，未重传素材、重做 AI 生成或放行失败素材。ZeroSSL 仅作为私有诊断证据保留，不是长期服务配置。
- 首次预签发曾将“新账户文件尚不存在”的正常 INFO 误判为失败并停止；精确修正后使用新的独立目录成功签发。未删除首轮证据，没有关闭真正错误的停止机制。
- 下一步定位 HTTPS 服务实现兼容性；代码仍未提审，正式版仍未发布。

## 已确认

- AppID：wx281b5275e4a1601f；小程序名称：暖笺 家书；个人主体。
- 备案已完成：皖ICP备2026032763号-1X；微信认证已完成。
- 1.0.0 已于 2026-09-23 21:19:50（北京时间）上传，包体 974,480 字节，微信后台显示为体验版。尚未提交代码审核，没有正式线上版本。
- 正式 API 为 https://api.warmjiashu.xyz/v1，request/uploadFile/downloadFile 合法域名均已配置。
- 当前后端运行 `4cf6a6f`（含 `09ab236` 回调诊断修复），模式为 `production + SQLite`；回调由正式 API 承接。SQLite 起于本轮新库，旧内存数据迁移未成功，故障及用户继续指示见下文。
- 分支为 `codex/warm-letter-mvp`；API 全量 361 项测试通过，小程序 237 项测试及类型检查通过；当前部署提交 `4cf6a6f` 的 [CI 已成功](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/35867865105)。

## 本轮完成

1. SQLite WAL/FULL、事务、幂等约束、会话持久化及中断任务恢复；原先依赖内存对象引用的修改全部显式保存。
2. 家书列表跨设备获取；单封删除和账号注销；本人跨家书回复清除；数据库删除与文件清理队列一致提交。慢生成、上传、朗读和审核回调不能重建删除的数据。
3. 微信真实文本审核、稳定令牌、图片/录音异步审核、AES 回调认证。受理不等于审核通过，只有通过后才能分享；文本生成前后与朗读输入均检查。
4. 素材规范化保护 M4A/Ogg 兼容，审核与播放使用同一 MP3。官方审核未列出的图片格式拒绝，避免改扩展名假装支持。审核准备失败可以重试，原始数据不假装已通过。
5. 新隐私页、数据管理页、原生隐私授权及正式环境约束。阅读别人分享无需登录；回复需要微信登录，“已登录”不代表认证真实姓名。
6. 新人气泡、预览、改稿、朗读、超时恢复沿用此前已验证功能；本轮审核发布步骤新增 60 秒总时限，服务生成新增 165 秒总体上限。

## 运维验证

- 服务器已安装 Ubuntu 官方 ffmpeg，实际队友 M4A 转码通过：148876B → 36045B，589ms。未据此宣称完整 AI 生成耗时为 0.6 秒。
- 切换前，候选目录隔离构建通过正式配置及健康验证，该阶段未打开正式业务数据库。微信官方 stable_token 单次探测成功；无凭证写入代码或报告。
- 从活内存迁移、SQLite 一致备份、关联媒体复制、独立恢复及过期轮转曾用合成数据演练；这些演练未避免本次真实切换时的导出故障，不能用作迁移成功的证据。
- 单主机/单 API 进程与本机备份是当前边界；不能声称异地容灾或多进程任务调度完成。
- 2026-09-23 20:53（北京时间）消息推送配置成功：此前发生扫码授权过期及“系统繁忙”，未观察到微信真实验证 GET。将未提交的候选 EncodingAESKey 改为页面要求的 43 位纯字母数字规范 Base64、同步候选服务与私密交接文件后，用户在有效验证会话中保存成功；后台显示“服务器配置已完成”，设置页确认 URL 正确、安全模式、JSON。服务器收到三次对应的签名 GET，均返回 200，处理耗时 2–3ms。配对密钥的公网 AES 合成认证事件也返回 200/success。配置页提示数据格式最多 5 分钟生效；这些结果不等同于实际图片/录音审核回调已经完成。该配置阶段仅重启临时握手服务，主 API 当时未动；后续生产切换见下节。未取得微信保存接口的详细错误码，不将先前“系统繁忙”归因为已被完全证实的单一因素。

## 正式切换、迁移故障与当前验证

原计划保留旧内存记录后迁移至 SQLite，但导出脚本缺少保持事件循环存活的引用句柄（keepalive），旧进程退出后，内存中的家书、账号、会话及关联记录未能保留。本次旧数据迁移没有成功，不能记作“迁移完成”。用户已明确旧记录只有测试数据，并指示继续，因此正式服务启用了全新的 SQLite 数据库。导出脚本缺陷已修复，新增两项回归通过，API 类型检查通过；修复不能恢复已丢失的内存记录，也不等同于重做成功的真实迁移。

原始 `uploads` 已保全，备份约 15 MB，逐文件 `diff` 核对一致。文件保全不代表原家书、账号及素材关联已恢复，不能据此承诺旧记录或旧分享仍可使用。首次恢复时的主 API PID 为 `60351`，仅为当时记录；当前运行版本见本页“已确认”。

每日备份于 2026-09-23 21:09:31（北京时间）首次执行成功，并完成独立恢复校验；定时器下一次计划执行时间为 2026-09-24 03:23:55（北京时间）。该次成功证明当前新 SQLite 数据库的备份和恢复链路可用，不代表旧内存数据已经恢复，也不构成异地容灾。

2026-09-23 21:34:42（北京时间）的第二次备份覆盖两封新家书和 3 个关联媒体对象，独立数据库恢复校验通过。随后部署重启的数据库完整性正常，前后各项计数未减少；同一会话仍可鉴权，两封草稿、匿名读信和朗读引用仍可用。这证明本轮新建记录可持久保存，不改变此前旧内存迁移失败的事实。

开发者工具端口 `26943` 已恢复，CLI 登录检查成功。首页、隐私页、数据管理页模拟器截图检查通过，未观察到运行错误；模拟器通过真实微信账号完成登录。1.0.0 已于 2026-09-23 21:19:50（北京时间）上传，包体 974,480 字节，后台显示为体验版；手机端新包的素材、生成、朗读、分享及删除闭环仍未完整验证。

后台隐私补充文档已上传。用户明确确认后，2026-09-23 约 22:55–22:56（北京时间）已提交用户隐私保护指引和 UGC 声明：UGC 页面显示“已提交 / 内容已成功提交”，返回基本设置后显示“已声明”；同页用户隐私保护指引显示“审核中”。隐私指引已提交待平台审核，不能称为已审核通过或已正式生效。代码审核尚未提交，正式版尚未发布。

## 本轮真实服务验证与当前阻塞

- 纯文字流程通过真实微信登录调用正式 Qwen 服务，生成及预览通过，本次约 20 秒。使用“芊悦”音色生成朗读约 93 秒并成功；随后确认家书、匿名读取家书均通过，朗读音频请求返回 HTTP 200。
- 队友提供的裁剪真实照片与 8.895 秒语音已上传，素材状态均为 `READY`；Qwen 多素材家书生成成功，本次约 62 秒。
- 上述媒体审核回调的实际错误已明确为 `-1008`，按微信官方 `mediaCheckAsync` 文档表示下载失败；审核状态为 `failed`，确认返回 HTTP 503 / `CONTENT_SAFETY_UNAVAILABLE`。自有公网请求返回 200，MIME、文件头及证书检查正常。含这些素材的家书仍不能分享；上传 `READY`、Qwen 生成或自有下载成功均不能替代微信审核通过。
- Caddy 已加入 `default_sni api.warmjiashu.xyz` 并使用 RSA 2048 位证书。限制为 TLS 1.2、仅支持 RSA 证书的客户端，在有 SNI 和无 SNI 两种情况下均通过证书链与域名校验，公网 `/health` 正常。2026-09-23 13:57:46 UTC（北京时间 21:57:46）再次提交媒体安全检查，13:57:49 和 13:57:50 UTC（北京时间 21:57:49、21:57:50）的两条回调仍返回 `-1008`。这次兼容性调整尚未解决微信媒体下载失败，不能据此宣称 TLS 是根因或审核已经修复。
- 这些耗时是本轮单次观测，不代表平均耗时或服务承诺；朗读音频 HTTP 200 不等于手机扬声器播放验收。未完成手机新包的完整闭环，也未向微信好友自动发送消息。文档不记录私有家书、素材、账号 ID 或分享凭据。
- 23:11:58.759–23:12:58.827（北京时间）的完整网络抓取为 350 包、148,524 字节，无截断，纠正了 22:12–22:13 首轮诊断的因果推断：12 条同类连接在 ClientHello 后 0.012–0.185 毫秒即发出 FIN，全部早于服务器首个 TLS 数据（ClientHello 后 2.416–3.078 毫秒）。后续 RST 不能证明客户端收到证书后拒绝；实际握手签名为 PKCS#1/SHA-512、曲线 P-256，均在客户端声明范围内。两条审核记录在 23:12:34.312 和 23:12:36.877（北京时间）仍更新为 `failed/-1008`，所有 HTTP 方法的审核素材请求为 0。当前不能确认相关连接的客户端身份或业务失败阶段，证书/网络根因未定；继续核查 URL 与下载链路。两组国内 DNS 查询均指向当前服务器。详细边界见[内容安全排查记录](./WECHAT_CONTENT_SAFETY.md)，未公开原始网络数据。

## 发布顺序与未完成项

1. 排查微信媒体下载错误 `-1008`，修复后重新验证媒体审核通过、确认及匿名阅读。保持审核失败时禁止分享，不以自有公网下载成功替代微信审核结果。
2. 跟进用户隐私保护指引的审核结果，当前后台显示“审核中”，尚不能认定已正式生效。UGC 声明已提交，基本设置显示“已声明”。公开联系邮箱已获授权；已上传的 `PRIVACY_SUPPLEMENT_1_0_0.txt` 说明香港处理、百炼、分享范围、注销和最多 7 天备份保留；如平台要求补充材料，按实际结果整改。
3. 使用已上传的 1.0.0 体验版完成手机端素材、生成、预览、朗读、分享、回复及删除验收；本轮真实服务调用和模拟器检查不能代替手机闭环。不向微信好友自动发消息。
4. 修复上述阻塞并完成手机验收后，核对最终上传包，填写真实功能、UGC/隐私与审核说明提交。现有备忘录类目不代表 AI 场景自动获准，审核要求以微信实际页面为准。
5. 等代码审核通过后正式发布，并记录后台实际结果。如果审核要求补充材料，按结果整改，不宣称已经发布。

## 隐私授权弹窗依据与验证边界

2026-09-23 核对微信[《小程序隐私协议开发指南》第六节](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/PrivacyAuthorize.html)：官方提供“无需开发者适配开发，自动向 C 端用户展示”的隐私弹窗；隐私接口触发授权事件后，若开发者未响应，“微信将主动弹出官方弹窗”。[wx.requirePrivacyAuthorize 文档](https://developers.weixin.qq.com/miniprogram/dev/api/open-api/privacy/wx.requirePrivacyAuthorize.html)说明该 API 用于“模拟隐私接口调用，并触发隐私弹窗逻辑”。

当前实现主动调用 `wx.requirePrivacyAuthorize`，不注册 `wx.onNeedPrivacyAuthorization`，使用上述官方弹窗；无需再添加自定义同意按钮。如果以后接管该事件，必须按[事件文档](https://developers.weixin.qq.com/miniprogram/dev/api/open-api/privacy/wx.onNeedPrivacyAuthorization.html)提供 `open-type="agreePrivacyAuthorization"` 按钮，并调用 `resolve({ event: 'agree', buttonId })` 或 `resolve({ event: 'disagree' })`，不能仅隐藏界面。

单元测试验证等待、拒绝、超时和重试后的应用行为，不能证明微信原生弹窗已在真机出现。正式验收仍须用未授权账号检查：出现指引、同意后继续、拒绝后不上传且仍可阅读分享；后台隐私声明需已生效。微信官方规定拒绝后不足 10 秒再次调用将直接报拒绝，不应自动循环弹窗。

## 1.0.0 提审文案草稿

状态：1.0.0 已上传并显示为体验版，未提审、未正式发布。正式后端已启用新 SQLite，纯文字真实生成、预览、朗读及匿名读信通过；含照片/语音的确认流程因媒体安全审核不可用被阻止，手机新包完整闭环待验收。微信后台消息推送配置的管理员扫码已通过，不代表小程序真机验收完成。以下为修复和验收完成后的填写文案，不能用文案代替验收结果；后台若要求声明 AI、用户生成内容或补充资质，应如实填写。

**版本说明：**

> 1.0.0：支持主动添加文字、照片和录音，使用 AI 整理家书草稿；提供来源核对、修改与重新生成、完整预览、可选 AI 朗读和微信分享。新增隐私与数据管理、家书删除、账号注销、内容审核及超时恢复。

**功能与 AI 使用说明：**

> 暖笺帮助用户将生活近况整理为写给亲友的家书。用户主动提交的必要内容由阿里云百炼 Qwen 进行图片理解、语音转写、文字整理与可选语音合成。草稿和合成声音明确标注 AI，用户核对修改后才能准备分享。本版本不生成图片、不克隆本人声音、不读取微信好友列表或聊天记录。选择微信好友由原生转发面板完成。

**审核操作步骤：**

1. 从 `pages/home/index` 点“写一封家书”，按微信提示授权并登录，无需另外注册账号或测试密码。
2. 在“直接写下一点近况”填写“今天整理了书桌，晚上打算做一碗面”，点“加入这段文字”和“下一步”；称呼填写“妈妈”，选择语气和篇幅，点“生成家书草稿”。
3. 在编辑页检查正文及内容依据；可修改文字、核对素材或将本人新增事实标记为“本人补充”，也可点“换一版文字”。点“预览家书”查看完整效果。
4. 可选择音色并生成、试听 AI 朗读；点“确认内容，准备寄出”，审核通过后点“选择微信好友寄出”。仅由操作者自主选择接收人。
5. 使用实际分享进入阅读页，核对大字阅读和已有朗读；阅读无需登录，送出回复需微信登录。体验版接收人须具备体验权限，普通好友可访问性须在正式发布后验证。
6. 另建一封家书，主动选择一张本人有权使用的 JPG/PNG/BMP 图片或录制一段简短语音；核对识别结果后完成上述流程。图片/录音异步审核待定时不能分享，可保留草稿稍后查看。
7. 首页“隐私与数据管理”查看隐私说明、删除测试家书并检查旧分享失效；如需验证注销，仅使用可删除测试数据的账号，确认后删除账号相关数据。手机原文件和微信账号不受影响。

入口路径：`pages/home/index`、`pages/settings/index`、`pages/privacy/index`。素材、编辑、预览页依赖当前创作上下文；阅读页依赖真实分享的家书 ID 与凭据，不应填写伪造的固定参数。

**内容与隐私说明：**

> 发布前检查文本及用户上传的图片、录音；审核未通过或未返回时不开放分享。朗读生成前检查输入文字，不据此声称合成音频本身通过了微信媒体审核。家书分享凭据持有人可阅读及转发，删除撤销相关链接，无法撤回接收方已保存的内容。应用服务器位于香港，必要内容传给百炼；提供单封删除和账号注销，滚动备份最长保留 7 天，联系邮箱为 1127563417@qq.com。

当前类目“工具 > 备忘录”不等于微信已认可本版本全部 AI 与分享功能；是否需要调整类目或补充材料，以实际提审页面和审核结果为准。以上步骤目前是验收计划，不是已完成测试的声明。

## 操作入口

- 通用迁移/备份工具：`scripts/production/README.md`。
- 微信审核接口及边界：`docs/WECHAT_CONTENT_SAFETY.md`。
- 已有体验版记录：`docs/MINIPROGRAM_0_2_3_DELIVERY_2026-09-19.md`。
- 本地私密临时材料统一放 D 盘；服务器配置、迁移快照、数据及用户素材不得进入 GitHub。
