# 真实多模态素材探针记录（2026-09-18）

## 目的

用一组已授权的受控素材验证暖笺的真实 Qwen 链路，而不是只用合成 fixture：

- 一段裁切后的生活照片（只保留货架、商品和价签）
- 一段示例 M4A 语音
- 一条文字近况

原始照片、音频和服务端密钥均未进入 Git；远端探针目录在测试完成后已删除。

## 运行配置

| 项目 | 结果 |
| --- | --- |
| API 环境 | `demo`，非生产 |
| 生成模型 | `qwen3.8-flash` |
| 语音转写模型 | `qwen3.5-omni-flash` |
| 媒体输入 | 图片原生理解，M4A 流式转写 |
| 代码版本 | `56c587b`（事实约束提示修订） |
| 运行时 | Node `22.23.2` |

## 结果

第二次真实探针在 `114630 ms`（约 114.6 秒）完成，返回三段家书。三个来源 ID 均出现在最终 `sourceRefs` 中：

- 文字素材被写入近况段落。
- 音频正确提取出“开会有点累、外卖附送饮品让人开心”的主要语义。
- 图片读到了货架、魔芋爽、包装文字和 `9.9 元/盒` 价签。
- 正文没有描述或猜测照片右侧路人。

完整脱敏前的本地结果保存在 D 盘临时目录，未提交到仓库。结果摘要示例：

```text
provider: openai-compatible-chat:qwen3.8-flash+audio:qwen3.5-omni-flash
source coverage: text + photo + audio
paragraph count: 3
```

## 未通过项

这次不能标记为“事实零扩写通过”。模型仍把音频里的“开会有点累”写成了“开了个长会”，并加入了“居然”这样的态度副词。两者都没有得到素材的直接支持。

本轮已完成：

- 在草稿和审校两次调用中加入原话/事实清单约束。
- 要求不升级程度、数量、时长、因果和不确定表达。
- 增加 API 回归测试，确认约束会发给生成和审校请求。

但提示词不是硬事实证明；真实 Qwen 结果仍显示需要 claim-level 证据门禁或本地确定性清理，才能把“可生成”升级为“可放行”。在完成该门禁前，不应把任意用户案例宣传为事实安全已验收。

## 服务清理与部署注意

- 探针结束后已删除服务器 `/tmp/warm-letter-case001-probe` 及临时编译文件。
- `https://api.warmjiashu.xyz/health` 复核返回 200，服务仍为 `demo + WeChat + Qwen profile-match`。
- 为完成本次探针，非生产服务暂时直接替换了 `apps/api/dist/ai.js`；服务器 Git 源码仍需在网络恢复后通过正常提交/部署流程同步 `56c587b`，避免源码与运行产物漂移。

## 下一步

1. 设计并测试逐句 claim/evidence 结构，或增加保守的本地事实门禁；不能只依赖文案提示。
2. 用相同受控素材复测，要求“长会”等扩写消失后才记录为事实审校通过。
3. 正常推送 GitHub 后重新部署完整提交，再进行一次微信体验版上传、真机生成和恢复流程测试。

