# 暖笺 2026-08-16 阶段进度、计划与问题

- 更新时间：2026-08-16（Asia/Shanghai）
- 当前分支：`codex/warm-letter-mvp`
- 当前已提交 HEAD：`2e0214f1f85299014e0b99089f5ffe2d98904b48`
- 当前工作项：`FIX-015` Demo、测试、比赛取证与生产环境强隔离
- 仓库状态：FIX-015 实现与阶段文档已形成本地可追溯提交 `2e0214f`；当前分支尚未 push，尚未创建 PR。GitHub 远端拒绝原因是 CLI OAuth token 缺少 `workflow` scope。
- 固定运行时：Node.js `22.23.2`、pnpm `11.19.0`
- 临时目录：`D:\tmp\warm-letter-ai-family`；测试、构建、截图和缓存不得在 C 盘另建任务临时目录
- 干净候选副本：`D:\tmp\warm-letter-ai-family\candidate-frozen-20260816`

## 1. 阶段结论

暖笺已经具备可离线运行的微信小程序、Fastify API 和 React H5 骨架，W1 本地代码门禁与 W2 本地/单实例分享安全门禁已有历史基线证据。当前工作树正在收口 `FIX-015`：三端已经建立显式环境模式、环境标识、服务端握手和 Demo 数据隔离，H5 主要浏览器状态与多视口矩阵已经完成阶段性验收。小程序向预签名上传地址泄露登录 Bearer token 的风险已经返修，外部 PUT 现在只发送 presign 明确返回的上传 headers；API 已改为短期、限定素材与 MIME 的上传专用凭据，并完成缺失、篡改、错素材、错 MIME、过期、重放和真实 HTTP listener 负向回归。

固定 Node.js `22.23.2` 下，最新全量结果为 API `113/113`、Web `38/38`、小程序 `28/28`、contracts `11/11`；根级 `pnpm check`、`pnpm build` 和 Web production bundle verifier 均通过。D 盘干净候选副本还完成了 `pnpm install --frozen-lockfile`、独立 pnpm store 安装、全仓检查、构建和 production bundle verifier。上传安全返修已达到代码与自动化测试收口条件，但 200% 字体、最长中文、无媒体状态、最终静态终检以及微信开发者工具/真机验收尚未完成，因此 **FIX-015 仍为 P0 验证中**。

当前不得宣称真实 OpenAI 四素材闭环、微信双设备闭环、G2、MVP、公测或生产已经完成。现阶段可对外表述仅限于“开发/比赛演示骨架可运行，环境隔离正在验收”。

## 2. 当前进度

### 2.1 已有稳定基线

| 范围 | 当前事实 | 证据边界 |
| --- | --- | --- |
| W1 本地代码 | 已验证基线 `7f5421b3f16e16dcc7b6164f66fd2765ce7bc22e`，tree `2633a456ba5afa5f8210e7956a0f79d6c82c7173` | 干净 D 盘检出完成 frozen install、全仓检查和四工作区构建；不代表真实供应商通过 |
| W2 分享安全 | 代码基线 `d1c525db521ac857866c019ac9dc264396c006e3`，H5 浏览器基线 `623811bad79748b2b5910614681ac3b2ce4308d3` | 仅放行本地/单实例分享、媒体和回复安全；不代表生产基础设施通过 |
| GitHub | GitHub CLI 已登录 `KamiZackJ`，远端 `KamiZackJ/warm-letter-ai-family` 已识别 | 本地提交 `2e0214f` 已形成；push 被缺少 `workflow` scope 的 OAuth token 阻塞，尚无 PR 或远端 Actions 证据 |
| 设计与验证技能 | 已应用 `web-design-guidelines`，并把 `playwright` 纳入 H5 验收方案；本机还可用 OpenAI Figma 系列技能 | Figma/Stitch 依赖 MCP；已确认 Apache-2.0 外部候选 `pbakaus/impeccable`、`google-labs-code/stitch-skills`，尚未安装，需先完成内容、供应链和场景适配审查；Web/React 规则不得原样套用到原生微信小程序 |

### 2.2 FIX-015 已进入工作树的实现

| 端 | 已实现内容 | 当前验证状态 |
| --- | --- | --- |
| API | 新增 `demo / test / competition / production`；competition 强制 OpenAI、凭据、模型和稳定媒体签名密钥；开发适配仍存在时 production 拒绝启动；`/health` 返回环境和非敏感能力摘要；presign 返回短期 `x-warm-letter-upload-token`，上传端点不再依赖暖笺登录 Bearer | 固定 Node 22.23.2 下全量 `113/113` 通过；覆盖上传凭据缺失、篡改、错素材、错 MIME、过期、完成后重放、登录 Bearer 单独上传失败、日志脱敏和真实 HTTP listener 链路 |
| H5 | 新增纯配置解析、分享参数校验、服务端环境握手和常驻环境标识；非 Demo 缺分享参数时显示错误；Demo 回复明确标记未发送；production 要求非回环 HTTPS API；Demo 素材仅在 Demo 构建复制 | 固定 Node 22.23.2 下 `38/38` 通过；production bundle verifier 在工作树和 D 盘干净候选副本均通过；浏览器补充矩阵仍待完成 |
| 小程序 | 新增四模式矩阵和环境存储命名空间；mock 仅允许 test；`develop -> demo`、`trial -> competition`、`release -> production`；非 Demo 拒绝 `demo=1`；五页常驻环境标识；真实请求前校验 `/health` 环境一致性；上传 PUT 只透传 presign headers | 固定 Node 22.23.2 下类型检查与全量 `28/28` 通过；上传链路测试证明 presign/complete 保持 API Bearer 鉴权，外部 PUT 仅含内容类型和上传签名头，不含 Authorization/Cookie。微信开发者工具与真机仍待验收 |
| Demo 素材 | 已加入本地合成 PNG/WAV，并在素材台账中明确标为演示素材 | 文件已进入工作树；存在、非零、PNG/WAV magic、SHA-256 和 README 台账测试已有阶段性本地通过记录；打包读取、上传链路和真机证据待完成 |
| 全仓候选 | 依赖、检查与构建均使用固定 Node 22.23.2 和 pnpm 11.19.0，缓存与产物位于 D 盘 | `pnpm install --frozen-lockfile`、根级 `pnpm check`、`pnpm build`、contracts `11/11` 和 production bundle verifier 通过；本地提交 `2e0214f` 已绑定，远端 CI 仍待 push 后验证 |

Demo 素材固定校验值：

- `synthetic-cooking-demo.png`：`92EADFCCE54996F5B56ACF8DB50F06E3A77A4D17A96FA1467F7BFEBEF576705C`
- `synthetic-voice-demo.wav`：`62253B93103723DB78A22AE87C51485BFF25B1EADA3585EBBEC528AD6A903B7F`

这些素材只用于 Demo，不得作为真实用户、真实家庭或真实 AI 调用证据。

### 2.3 本阶段已完成、待冻结快照复验

1. H5 Demo 的标题、正文、事实和合成图片已统一为“第一次做番茄炒蛋”场景。
2. API competition 已要求 HTTPS，并拒绝 `localhost`、`localhost.`、子域 `localhost`、完整 `127.0.0.0/8`、`0.0.0.0`、IPv6 回环、IPv4 映射 IPv6 回环和通配主机；门禁同时覆盖 `PUBLIC_BASE_URL` 与 `CORS_ORIGINS`。
3. 小程序 competition/production 已拒绝空值、纯空白和大小写不敏感的 `touristappid`。
4. Demo 素材测试已固定验证文件存在、非零、PNG/WAV magic、SHA-256 和 README 中的 hash 台账。
5. 小程序 `readFile -> uploadBinary -> complete` 自动化链路已经完成安全返修：presign/complete 继续携带 API Bearer，外部 PUT 只发送 presign headers，并用负向断言确认没有 Authorization/Cookie；全量 `28/28` 与 typecheck 已通过。
6. H5 已用 Playwright 覆盖 Demo、非 Demo 无参数/半参数、远程内容、`/health` 失败与非法 JSON、环境不匹配、reader 404/410/500，以及 320/390/768/1440 视口的溢出和素材解码检查；证据保存在 `D:\tmp\warm-letter-ai-family`。
7. D 盘干净候选副本已完成 frozen install、全仓检查、全仓构建和 production bundle verifier；工作树的 API `113/113`、Web `38/38`、小程序 `28/28`、contracts `11/11` 均通过。

## 3. 当前问题

### 3.1 P0：关闭 FIX-015 前必须解决

1. H5 主要浏览器状态和 320/390/768/1440 视口已经验收；200% 字体、最长中文文案和无媒体状态仍需补齐，最终复跑时还需确认 favicon 与 `/health` 最新调整未引入回归。
2. D 盘候选副本的依赖、全仓检查、构建和 bundle verifier 已通过；最终文档调整后的严格 UTF-8、密钥扫描、Markdown 链接、Demo 素材 magic/hash、production bundle Demo 哨兵和 `git diff --check` 终检也已通过。当前仍需把这些结果绑定到可追溯提交 SHA 与远端 CI。
3. 小程序自动化上传安全回归已通过，但打包后的 `wx.getFileSystemManager().readFile -> uploadBinary -> complete -> reader` 仍缺微信开发者工具和真机证据。
4. 小程序仍缺开发者工具/真机对五页环境标识、打包素材读取、上传、预览、播放、删除、API 断开后的持久错误状态和非 Demo 绕过拒绝的证据。

### 3.2 P0：不由 FIX-015 自动关闭

| 问题 | 影响 | 关闭证据 |
| --- | --- | --- |
| 缺真实 OpenAI 凭据、模型权限、额度和可用网络 | W1 真实供应商门禁未通过 | 脱敏 request ID、模型、usage、耗时、成功/失败和重试记录 |
| 缺授权照片、截图、中文语音、文字与人工事实基准 | 无法证明真实四素材事实和来源引用正确 | 素材 SHA-256、授权台账、人工事实表与草稿逐项对照 |
| 缺微信 AppID、正式 `code2Session`、可达 HTTPS API、体验成员和两台设备 | W3 真机与双设备闭环未通过 | 开发者工具/真机录像、请求时间线、错误 token 和两端刷新证据 |
| 删除生命周期仍不完整 | 禁止公测和生产 | 分享立即失效、在线副本清理、失败补偿、备份和第三方处理证据 |
| 正式内容审核尚未接入 | 禁止公开 UGC | 审核供应商、超时 fail-closed、用户修正、申诉和人工处理流程 |
| 参赛资格、目标期次、学校要求、快手账号与小程序主体证据未归档 | 可能影响报名和发布计划 | 官方页面、报名/学校确认、账号和主体截图 |

### 3.3 P1：技术和产品风险

1. 仓储、任务和限流仍以单进程实现为主；生产需要 PostgreSQL/Redis、持久队列、跨实例原子约束和可信代理配置。
2. API、H5 和小程序仍有私有 DTO/schema，`FIX-012` 尚未完成全量共享契约与微信构建兼容性验证。
3. H5 320/768/200% 完整矩阵、无媒体状态、最长中文文案，以及小程序读屏、系统大字号和触控热区仍需视觉/真机复验。
4. 长图、短片、匿名事件字典、跨设备历史/署名、用户改写后的来源一致性和比赛逐镜头证据仍未完成。
5. 远端 GitHub Actions 尚未运行；本地通过不能替代 CI、真机或发布门禁。

## 4. 推进计划

| 顺序 | 状态 | 负责人 | 工作 | 完成证据 / 下一动作 |
| --- | --- | --- | --- | --- |
| 1 | 已完成 | API 实现 + 安全 QA | 收口上传专用凭据、真实 HTTP listener、日志脱敏、重放边界和外部 PUT 头部白名单 | API `113/113`、小程序 `28/28`；登录 Bearer 单独不能上传，外部 PUT 无 Authorization/Cookie |
| 2 | 已完成 | 队长 + 实现/QA | 在固定 Node 22.23.2 下建立 D 盘干净候选副本并复验全仓 | frozen install、`pnpm check`、`pnpm build`、contracts `11/11` 和 production bundle verifier 通过 |
| 3 | 进行中 | 浏览器 QA | 用 Playwright 补齐 200% 字体、最长中文和无媒体状态，并复跑 favicon、`/health` 分类及核心桌面/移动场景 | 形成截图、控制台/网络记录、非空像素和无横向溢出记录；回归则立即返修 |
| 4 | 已完成 | 队长 + 静态 QA | 完成严格 UTF-8、密钥、Markdown 链接、素材 magic/hash、bundle Demo 哨兵和 diff 终检 | 严格 UTF-8 58/58、Markdown 30/30、密钥高置信扫描 0 命中、素材校验和 production bundle verifier 均通过；产物位于 D 盘临时目录 |
| 5 | 待外部条件 | 微信真机 QA | 在开发者工具和真机验证五页环境标识、非 Demo 绕过拒绝、素材读取/上传/预览/播放/删除、API 断开和错误 token | 需要 AppID、可达 HTTPS API、体验成员与真机；条件不足时 W3 不放行 |
| 6 | 进行中 | 队长 + 独立项目经理 | 独立复核绕过路径、对外表述和证据索引，确认 FIX-015 是否可关闭 | 汇总复核结论、未关闭 P0/P1 和返修记录；任一 P0 仍成立则继续返修 |
| 7 | 已完成（远端待授权） | 队长 | 返修收口后形成可追溯本地提交；push 前要求 GitHub CLI 具备 `workflow` scope | 提交 `2e0214f1f85299014e0b99089f5ffe2d98904b48`、树对象和本地测试已固定；补 scope 后再 push，不能把本地完成表述为远端完成 |

阶段汇报继续采用固定格式：目标、完成事实、证据、未完成项、风险变化、返修结果、下一阶段前三项。影响里程碑的阻塞超过 4 个工作小时，由项目经理升级并给出可并行推进的替代任务；项目经理可按风险增开独立 QA、设计审查或证据整理子任务。

## 5. 当前放行判断

| 门禁 | 状态 | 说明 |
| --- | --- | --- |
| W1 本地代码 | 已通过 | 仅绑定已验证历史基线 |
| W1 真实供应商 | 未通过 | 缺真实凭据、授权四素材和人工事实基准 |
| W2 本地/单实例分享安全 | 已通过 | 不覆盖生产持久化、共享限流和正式审核 |
| FIX-015 环境强隔离 | 验证中 | 上传专用凭据、自动化全量回归和最终静态终检已完成；剩余浏览器补充矩阵及微信开发者工具/真机证据未完成 |
| W3 微信双设备 | 未通过 | 缺 AppID、正式鉴权、可达 API、两台设备和录像 |
| W4 证据汇总 | 未完成 | 等待 W1 真实供应商、FIX-015 与 W3 |
| G2 / MVP | 未放行 | 核心真实证据仍不完整 |
| 公测 / 生产 | 未放行 | 持久化、审核、删除、运维、合规和正式部署仍未关闭 |

## 6. 需要团队提供或确认

1. OpenAI 项目凭据、目标模型、权限/额度和执行真实调用的授权。
2. 一组团队自有、合成或明确授权的照片、截图、中文语音和文字，以及人工事实基准。
3. 微信 AppID、体验成员、可达 HTTPS API 和两台测试设备安排。
4. iCAN 目标期次、校内截止、参赛成员/指导老师要求、快手账号控制权和小程序主体信息。
