# 暖笺 2026-08-16 阶段进度、计划与问题

- 更新时间：2026-08-16（Asia/Shanghai）
- 当前分支：`codex/warm-letter-mvp`
- 当前状态基线：`18027406f7d0683526e7664a41f27625e5a97a18`
- FIX-015 历史主实现提交：`2e0214f1f85299014e0b99089f5ffe2d98904b48`
- 本轮本地验证实现提交：`18027406f7d0683526e7664a41f27625e5a97a18`
- 当前工作项：固化重复 PUT 与适老返修的本地证据，再推进真实 OpenAI、微信真机和发布前门禁
- 仓库状态：本地实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 已完成验证，尚待推送；[草稿 PR #1](https://github.com/KamiZackJ/warm-letter-ai-family/pull/1) 的历史远端 HEAD `71d6aae` 两条 `verify` 均已通过，但不覆盖本轮实现。推送后必须以 PR 最新 HEAD 全绿为准。
- 固定运行时：Node.js `22.23.2`、pnpm `11.19.0`
- 临时目录：`D:\tmp\warm-letter-ai-family`；测试、构建、截图和缓存不得在 C 盘另建任务临时目录
- 干净候选副本：`D:\tmp\warm-letter-ai-family\candidate-frozen-20260816`

## 1. 阶段结论

暖笺已经具备可离线运行的微信小程序、Fastify API 和 React H5 骨架，W1 本地代码门禁与 W2 本地/单实例分享安全门禁已有历史基线证据。本轮实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 收口了 `FIX-015` 的本地返修：三端环境隔离已建立；上传对象改为原子首次写入；Web 移动端正文 20px 与小程序删除确认、ARIA 状态和 88rpx 触控热区已完成。

固定 Node.js `22.23.2` 下，实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 的本地结果为 API `114/114`、Web `38/38`、小程序 `30/30`、contracts `11/11`；根级 `pnpm check`、`pnpm build` 和 Web production bundle verifier 均通过。重复 PUT 防覆盖及 Web/小程序适老返修已完成本地复验，H5 证据已写入 [`UI_ACCESSIBILITY_RETEST.md`](./evidence/2026-08-16/UI_ACCESSIBILITY_RETEST.md)。远端 CI 尚未针对该提交运行，微信开发者工具/真机证据也未完成，因此 **FIX-015 仍为验证中**。

当前不得宣称已达到 G1、G2、MVP、公测、生产或比赛提交候选；真实 OpenAI 四素材闭环和微信双设备闭环也未完成。现阶段可对外表述仅限于“开发/带永久 Demo 标识的演示骨架可运行，本地安全与适老返修已通过自动化复验”。

项目经理当前判断：本地环境隔离、上传鉴权、原子首次写入和适老代码返修已绑定实现提交 `18027406f7d0683526e7664a41f27625e5a97a18`，但尚待推送后的最新远端 CI；真实 AI、微信真机/双设备、完整删除、正式审核和生产基础设施仍未关闭。

## 2. 当前进度

### 2.1 已有稳定基线

| 范围 | 当前事实 | 证据边界 |
| --- | --- | --- |
| W1 本地代码 | 已验证基线 `7f5421b3f16e16dcc7b6164f66fd2765ce7bc22e`，tree `2633a456ba5afa5f8210e7956a0f79d6c82c7173` | 干净 D 盘检出完成 frozen install、全仓检查和四工作区构建；不代表真实供应商通过 |
| W2 分享安全 | 代码基线 `d1c525db521ac857866c019ac9dc264396c006e3`，H5 浏览器基线 `623811bad79748b2b5910614681ac3b2ce4308d3` | 仅放行本地/单实例分享、媒体和回复安全；不代表生产基础设施通过 |
| GitHub | 本地实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 已创建；[草稿 PR #1](https://github.com/KamiZackJ/warm-letter-ai-family/pull/1) 已打开 | 历史远端 HEAD `71d6aae` 两条 `verify` 通过；`1802740` 尚待推送和最新远端 CI |
| 设计与验证技能 | 已应用 `web-design-guidelines`，并把 `playwright` 纳入 H5 验收方案；本机还可用 OpenAI Figma 系列技能 | Figma/Stitch 依赖 MCP；已确认 Apache-2.0 外部候选 `pbakaus/impeccable`、`google-labs-code/stitch-skills`，尚未安装，需先完成内容、供应链和场景适配审查；Web/React 规则不得原样套用到原生微信小程序 |

### 2.2 FIX-015 已进入工作树的实现

| 端 | 已实现内容 | 当前验证状态 |
| --- | --- | --- |
| API | 新增 `demo / test / competition / production`；competition 强制 OpenAI、凭据、模型和稳定媒体签名密钥；开发适配仍存在时 production 拒绝启动；presign 返回短期上传凭据；文件系统对象使用原子首次创建，拒绝完成前重复 PUT 覆盖 | 固定 Node 22.23.2 下全量 `114/114` 通过；重复 PUT、并发双 PUT 和真实 HTTP listener 均验证仅一次 204，其余稳定 409，首次字节保持不变 |
| H5 | 新增纯配置解析、分享参数校验、服务端环境握手和常驻环境标识；非 Demo 缺分享参数时显示错误；Demo 回复明确标记未发送；production 要求非回环 HTTPS API；Demo 素材仅在 Demo 构建复制；回复输入框适配 200% 字体 | 实现提交 `1802740` 本地 `38/38` 通过；production bundle verifier 与 320/390/768/1440、200% 字体、最长中文、无媒体状态检查通过 |
| 小程序 | 新增四模式矩阵和环境存储命名空间；上传 PUT 只透传 presign headers；素材删除增加确认/取消；意图、编辑和素材输入补 ARIA，分段控件补 `aria-pressed`，次要操作热区提升到至少 88rpx | 固定 Node 22.23.2 下类型检查与全量 `30/30` 通过；删除取消/确认、上传头部白名单和环境隔离均有自动化。微信开发者工具、系统大字号、读屏和真机点击仍待验收 |
| Demo 素材 | 已加入本地合成 PNG/WAV，并在素材台账中明确标为演示素材 | 文件已进入工作树；存在、非零、PNG/WAV magic、SHA-256 和 README 台账测试已有阶段性本地通过记录；打包读取、上传链路和真机证据待完成 |
| 全仓候选 | 依赖、检查与构建均使用固定 Node 22.23.2 和 pnpm 11.19.0，缓存与产物位于 D 盘 | 历史 `2e0214f` 的 API `113/113`、Web `38/38`、小程序 `28/28`、contracts `11/11` 保持不变；本地验证实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 为 API `114/114`、Web `38/38`、小程序 `30/30`、contracts `11/11`，远端 CI 待运行 |

Demo 素材固定校验值：

- `synthetic-cooking-demo.png`：`92EADFCCE54996F5B56ACF8DB50F06E3A77A4D17A96FA1467F7BFEBEF576705C`
- `synthetic-voice-demo.wav`：`62253B93103723DB78A22AE87C51485BFF25B1EADA3585EBBEC528AD6A903B7F`

这些素材只用于 Demo，不得作为真实用户、真实家庭或真实 AI 调用证据。

### 2.3 本阶段已完成、待提交与外部证据

1. H5 Demo 的标题、正文、事实和合成图片已统一为“第一次做番茄炒蛋”场景。
2. API competition 已要求 HTTPS，并拒绝 `localhost`、`localhost.`、子域 `localhost`、完整 `127.0.0.0/8`、`0.0.0.0`、IPv6 回环、IPv4 映射 IPv6 回环和通配主机；门禁同时覆盖 `PUBLIC_BASE_URL` 与 `CORS_ORIGINS`。
3. 小程序 competition/production 已拒绝空值、纯空白和大小写不敏感的 `touristappid`。
4. Demo 素材测试已固定验证文件存在、非零、PNG/WAV magic、SHA-256 和 README 中的 hash 台账。
5. 小程序 `readFile -> uploadBinary -> complete` 自动化链路保持安全：presign/complete 继续携带 API Bearer，外部 PUT 只发送 presign headers；删除确认/取消、ARIA 和触控热区返修后全量 `30/30` 与 typecheck 通过。
6. H5 历史矩阵继续覆盖 320/390/768/1440、200% 字体、最长中文和无媒体状态；本轮 20px 移动端返修在 320/390 宽度下无横向溢出、console error/warning 为 0，截图与校验值已归档到 [`UI_ACCESSIBILITY_RETEST.md`](./evidence/2026-08-16/UI_ACCESSIBILITY_RETEST.md)。原始 Playwright 会话仍只保存在 D 盘临时目录。
7. 历史 FIX-015 主实现提交 `2e0214f` 的 API `113/113`、Web `38/38`、小程序 `28/28`、contracts `11/11` 保持不变；本地验证实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 为 API `114/114`、Web `38/38`、小程序 `30/30`、contracts `11/11`。

### 2.4 本轮返修结果与证据边界

1. API 上传对象已改为原子“仅首次创建”；顺序重复 PUT、并发双 PUT 和真实 HTTP listener 均证明重复写入返回稳定 `409 UPLOAD_ALREADY_RECEIVED`，首次字节不被覆盖。
2. Web 移动端 `.letter-body` 已恢复为 `20px`；320/390 视口的计算字号、横向溢出、素材解码和控制台检查均通过。
3. 小程序素材删除已增加确认/取消，输入补 `aria-label`，分段控件补 `aria-pressed`，相关次要按钮达到至少 `88rpx`；新增行为测试后全量 `30/30` 与 typecheck 通过。
4. 上述结论只关闭本地代码和自动化返修，不替代微信开发者工具、系统大字号、读屏或真机点击验收，因此 `FIX-023`、W3、G1、G2 均保持未关闭。

## 3. 当前问题

### 3.1 P0：关闭 FIX-015 前必须解决

1. 浏览器历史矩阵与本轮移动端 20px 返修均已通过并绑定实现提交 `18027406f7d0683526e7664a41f27625e5a97a18`；原始 Playwright 会话、完整 console/page-state 仍在 D 盘临时目录，最终索引待补。
2. 实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 已完成本地全量复验；历史远端 HEAD `71d6aae` 的检查不覆盖该提交，必须推送后重新等待最新 HEAD 全绿。
3. 小程序自动化上传安全回归已通过，但打包后的 `wx.getFileSystemManager().readFile -> uploadBinary -> complete -> reader` 仍缺微信开发者工具和真机证据。
4. 小程序仍缺开发者工具/真机对五页环境标识、打包素材读取、上传、预览、播放、删除、API 断开后的持久错误状态和非 Demo 绕过拒绝的证据。

### 3.2 P0：不由 FIX-015 自动关闭

| 问题 | 影响 | 关闭证据 |
| --- | --- | --- |
| 缺真实 OpenAI 凭据、模型权限、额度和可用网络 | W1 真实供应商门禁未通过 | 脱敏 request ID、模型、usage、耗时、成功/失败和重试记录 |
| 缺授权照片、截图、中文语音、文字与人工事实基准 | 无法证明真实四素材事实和来源引用正确 | 素材 SHA-256、授权台账、人工事实表与草稿逐项对照 |
| 缺微信 AppID、正式 `code2Session`、可达 HTTPS API、体验成员和两台设备 | W3 真机与双设备闭环未通过 | 开发者工具/真机录像、请求时间线、错误 token 和两端刷新证据 |
| 删除生命周期仍不完整 | 禁止公测和生产 | 分享立即失效、在线副本清理、失败补偿、备份和第三方处理证据 |
| 正式内容审核尚未接入 | 禁止公开 UGC | 审核供应商、超时 fail-closed、用户修正、申诉和人工处理流程 |
| 参赛资格、目标期次、学校要求、快手账号与小程序主体证据未归档 | 可能影响报名和发布计划 | 官方页面、报名/学校确认、账号和主体截图 |

### 3.3 P1：技术和产品风险

1. 仓储、任务和限流仍以单进程实现为主；生产需要 PostgreSQL/Redis、持久队列、跨实例原子约束和可信代理配置。
2. API、H5 和小程序仍有私有 DTO/schema，`FIX-012` 尚未完成全量共享契约与微信构建兼容性验证。
3. H5 浏览器矩阵、20px 移动端复验、200% 字体、无媒体状态和最长中文文案已完成；小程序读屏、系统大字号和触控热区仍需视觉/真机复验。
4. 长图、短片、匿名事件字典、跨设备历史/署名、用户改写后的来源一致性和比赛逐镜头证据仍未完成。
5. 历史远端 HEAD `71d6aae` 的两条 GitHub Actions `verify` 已通过，但未覆盖实现提交 `18027406f7d0683526e7664a41f27625e5a97a18`；推送后必须重新等待最新 HEAD 全绿。
6. Web/小程序适老返修已通过本地自动化，小程序真机视觉、系统字号、读屏和点击仍未完成；`FIX-023` 不能关闭。
7. `ACCEPTANCE_CHECKLIST.md` 与 `PROJECT_STATUS.md` 已绑定本轮实现 SHA；仍需补最新远端 CI 和最终证据索引。
8. 文件系统对象独占创建成功后、metadata sidecar 完成前若进程被强制终止，仍可能留下孤儿对象或不完整 sidecar；这是生产崩溃一致性 P1，不重新打开重复 PUT 防覆盖的本地 P0。
9. 正式测试集尚未固化“对象已创建、metadata 写入前失败/崩溃”的故障注入与清理回归；生产化前需补恢复扫描、原子提交或等价补偿机制及确定性测试。

## 4. 推进计划

| 顺序 | 状态 | 负责人 | 工作 | 完成证据 / 下一动作 |
| --- | --- | --- | --- | --- |
| 1 | 已完成 | API 实现 + 安全 QA | 收口上传专用凭据、真实 HTTP listener、日志脱敏、重放边界和外部 PUT 头部白名单 | API `114/114`、小程序 `30/30`；登录 Bearer 单独不能上传，外部 PUT 无 Authorization/Cookie |
| 2 | 已完成 | 队长 + 实现/QA | 在固定 Node 22.23.2 下建立 D 盘干净候选副本并复验全仓 | frozen install、`pnpm check`、`pnpm build`、contracts `11/11` 和 production bundle verifier 通过 |
| 3 | 已完成 | 浏览器 QA | 用 Playwright 补齐 200% 字体、最长中文和无媒体状态，并复跑 favicon、`/health` 分类及核心桌面/移动场景 | D 盘截图、console/network 记录、非空像素和无横向溢出记录；回复输入框已按字号修正 |
| 4 | 已完成 | 队长 + 静态 QA | 完成严格 UTF-8、密钥、Markdown 链接、素材 magic/hash、bundle Demo 哨兵和 diff 终检 | 118 个文本文件严格 UTF-8 通过，15 个 Markdown 文件相对链接全部有效，密钥高置信真实命中 0（3 个 `example.test` 为负例夹具），素材校验和 production bundle verifier 均通过；产物位于 D 盘临时目录 |
| 5 | 待外部条件 | 微信真机 QA | 在开发者工具和真机验证五页环境标识、非 Demo 绕过拒绝、素材读取/上传/预览/播放/删除、API 断开和错误 token | 需要 AppID、可达 HTTPS API、体验成员与真机；条件不足时 W3 不放行 |
| 6 | 已完成（本阶段） | 队长 + 独立项目经理 | 独立复核绕过路径、对外表述和证据索引，确认 FIX-015 是否可关闭 | 重复 PUT 与本轮 API/UI 返修已本地通过；最新提交/远端 CI、原始证据归档、微信真机、真实供应商和竞赛材料仍保持未放行 |
| 7 | 进行中 | 队长 | 推送本地实现提交并维护草稿 PR | `18027406f7d0683526e7664a41f27625e5a97a18` 已本地验证；推送后必须以 PR 最新 HEAD 全绿为准 |
| 8 | 已完成（本地） | API 实现 + 安全 QA | 将上传对象改为原子仅首次写入，重复 PUT 稳定返回 409，补首次字节不被覆盖的并发和真实 listener 回归 | API `114/114`；已绑定 `18027406f7d0683526e7664a41f27625e5a97a18`，远端 CI 待运行 |
| 9 | 已完成（本地） | Web/小程序实现 + 设计 QA | 修正 Web 移动端正文 20px、小程序删除确认、ARIA 状态和不足 88rpx 的触控热区 | Web `38/38`、小程序 `30/30`、typecheck/build 与 H5 320/390 复验通过；微信开发者工具和真机仍待完成，不能关闭 `FIX-023` |
| 10 | 已完成 | 项目经理 | 对齐验收清单和项目状态中的 H5、M0、M0-M7 与 GitHub 状态 | H5 完成事实与 D 盘证据边界已拆分；M0 降为证据不足；M0-M7 使用唯一含义；PR #1 和两条 `verify` 已记录 |
| 11 | 待外部条件 | 队长 + W1 QA | 使用授权四素材完成真实 OpenAI 端到端生成和事实对照 | 需要真实凭据、模型权限、额度、网络、授权素材和脱敏 request/usage 证据 |
| 12 | 待方案与部署 | 队长 + 项目经理 | 关闭正式内容审核、完整删除生命周期和生产持久化/共享限流/可信代理门禁 | 形成供应商、SLA、失败补偿、三次删除演练和生产部署证据后再评审公测/提交候选 |
| 13 | 待开发（P1） | API 实现 + 安全 QA | 处理对象与 metadata sidecar 在进程崩溃下的一致性，并固化故障注入清理用例 | 证明对象创建后任一中断点都可恢复或清理，不遗留可见孤儿对象；纳入正式测试集和生产运维检查 |

阶段汇报继续采用固定格式：目标、完成事实、证据、未完成项、风险变化、返修结果、下一阶段前三项。影响里程碑的阻塞超过 4 个工作小时，由项目经理升级并给出可并行推进的替代任务；项目经理可按风险增开独立 QA、设计审查或证据整理子任务。

## 5. 当前放行判断

| 门禁 | 状态 | 说明 |
| --- | --- | --- |
| G0 内部开发 | 已通过 | fake AI、本地存储、三端骨架和自动化可运行 |
| G1 内部演示 | 未达到 | Demo 路径可运行，实现提交 `1802740` 尚待最新远端 CI，且缺完整 UI 录屏和微信开发者工具证据 |
| W1 本地代码 | 已通过 | 仅绑定已验证历史基线 |
| W1 真实供应商 | 未通过 | 缺真实凭据、授权四素材和人工事实基准 |
| W2 本地/单实例分享安全 | 已通过 | 不覆盖生产持久化、共享限流和正式审核 |
| FIX-015 环境强隔离 | 验证中 | 本地实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 已通过；最新远端 CI、原始证据索引及微信开发者工具/真机证据仍未完成 |
| W3 微信双设备 | 未通过 | 缺 AppID、正式鉴权、可达 API、两台设备和录像 |
| W4 证据汇总 | 未完成 | 实现 SHA 已绑定，等待最新远端 CI、W1 真实供应商与 W3 真机证据 |
| MVP | 未达到 | 真实 AI、真机双设备、完整删除和跨端闭环证据仍不完整 |
| G2 用户测试 | 未放行 | 授权真实素材、稳定部署、正式审核和隐私/适老真机证据未齐 |
| 公测 / 生产 | 未放行 | 持久化、审核、删除、运维、合规和正式部署仍未关闭 |
| 比赛提交候选 | 未形成 | 尚无冻结 SHA、最新远端 CI、视频/PDF、报名回执、公开 Demo 和完整证据索引 |

## 6. 需要团队提供或确认

1. OpenAI 项目凭据、目标模型、权限/额度和执行真实调用的授权。
2. 一组团队自有、合成或明确授权的照片、截图、中文语音和文字，以及人工事实基准。
3. 微信 AppID、体验成员、可达 HTTPS API 和两台测试设备安排。
4. iCAN 目标期次、校内截止、参赛成员/指导老师要求、快手账号控制权和小程序主体信息。
