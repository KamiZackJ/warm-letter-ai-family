# 家书生成性能与等待体验说明

最后更新：2026-09-19（Asia/Shanghai）

## 结论

家书生成的主要等待来自真实 Qwen 链路：语音转写、草稿生成和事实复核。2026-09-18 分段实测原配置分别为 10.138、57.798、38.921 秒，总计 106.872 秒。关闭 Qwen 写作/复核默认开启的额外思考后，同素材首次对照为 60.248 秒（约缩短 44%）；仍执行两次写作/审校调用。这是单次对照，不是速度保证。

本轮已把问题拆成两部分处理：缩短可控制的处理时间，以及让不可避免的远端等待可被理解、可离开、可恢复。

此前开发者工具服务端口关闭的阻塞已经解除，`0.2.1` 已完成上传及体验版核实。本轮 `0.2.2` 已通过自动化验证、部署后端并上传；微信后台已核实其为当前体验版。本次修复流式语音转写长时间不结束时的停止与重试，真机失败恢复仍待复测。

## 已实现

### 客户端

- 相机照片在上传前通过 `wx.compressImage` 缩放到最长边 1600、质量 78；截图保留更高质量并缩放到最长边 2048。压缩失败会回退原文件，服务端仍执行 MIME、魔数和大小校验。
- 多张图片的压缩并行执行，减少串行等待和上传体积。
- 生成页显示阶段、已等待秒数和非终态进度：准备素材、理解图片/语音、整理家书、仍需一点时间。
- 等待 90 秒后可回首页；任务编号和幂等键仍保存在本机，后台任务不会被重复提交。
- 达到客户端轮询上限时，页面显示“已转到后台整理”，不再跳进编辑页触发第二次 180 秒等待。首页“最近家书”里的“生成中”记录可再次打开恢复。

### API 与模型调用

- 同一批媒体先并行读取，再做总字节数校验，避免逐个读取对象。
- Qwen 草稿与事实复核请求增加按篇幅限制的 `max_tokens`：短笺 720、标准 1050、长信 1500，避免 JSON 已完成后继续生成无用内容。
- 事实复核仍保留第二次多模态调用；不能为了追求表面速度而删除事实来源审校。
- 仅已验证 DashScope Qwen 档案发送 `enable_thinking: false`；其他兼容模型不附带该厂商参数。
- 显式拒绝非正常结束的模型输出；即使截断片段能解析为 JSON 也不接受。
- 私有编辑页展示 ASR 转写并允许更正。更正后重生成直接复用已确认文字，省掉这段音频的再次转写；旧稿在重生成成功前禁止发布和生成持久朗读。
- 图片段落及未确认语音段落标为待核对。识别仍可能错误，须由作者核对，来源 ID 覆盖不代表事实正确。

## 0.2.2：流式语音转写总时限

以下机制已通过自动化验证并随提交 `395aa8f4a4ecd75210f8fc246d78a55a10badef7` 部署：

- 每段流式 ASR 从发出请求到读取完整响应流，共用 `OPENAI_COMPATIBLE_TIMEOUT_MS` 的总时限；当前线上配置为 `60000` 毫秒。供应商陆续返回片段不会重新开始计时。
- 到期即中止请求，返回可重试的 `AI_PROVIDER_TIMEOUT`。已收到但未完成的部分转写全部丢弃，不进入后续写作。
- 该次流式 ASR 禁用 SDK 自动重试，避免重试退避或供应商 `Retry-After` 延长等待；写作、事实复核的现有重试规则不变。
- 客户端把这种终态错误提示为“本次生成已停止”，允许用户手动重试并保留已上传素材；不能继续显示成同一任务仍在后台处理。

此时限针对**单段流式 ASR**，不是整封家书 60 秒内完成的承诺。多段录音、媒体读取、草稿生成与复核仍分别耗时；此改动也不意味着正常生成再次提速。它没有修复内存任务与家书在服务器重启后丢失的问题。

### 验证与部署记录

- API `239/239`、小程序 `163/163` 测试通过；两端类型检查及 API、共享契约构建通过。
- ASR 专项包括真实 SDK 连接本地 HTTP 服务的 4 项测试及 9 项单元测试，验证总时限、中止和流处理边界。这些是受控自动化测试，不是本轮付费 Qwen 探针或真机记录。
- [GitHub CI run 35379112649](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/35379112649) 成功，覆盖 `pnpm check`、`pnpm build` 及生产 bundle 校验。
- 香港 API 已部署上述提交并重启；严格公网 HTTPS `/health` 检查正常，仍为 `demo + WeChat + Qwen profile-match`。服务端超时配置为 `60000`，全局 `MAX_RETRIES=1`，仅流式 ASR 每次请求指定 `maxRetries: 0`。
- 本机与服务器 `apps/api/dist/ai.js` 的 SHA-256 一致：`01fe0408b0d8d0e609af21c0645292a780ae48be21f7073132a3ccd59d3a78d6`。
- 小程序 `0.2.2` 上传成功，包体 907,549 字节；微信后台核实为当前体验版，提交时间 `2026-09-19 02:17:30`。后台仍无审核版本和线上版本，没有提交正式审核或发布。

## 延迟边界

供应商排队、网络、图片大小、录音长度与重试都会影响耗时。此前完整分段数据、事实问题定位及提速部署记录见 [真实多模态测试报告](REAL_MULTIMODAL_TEST_2026-09-18.md)。服务端提速已部署；当前 `0.2.2` 体验版保留等待恢复与语音纠错界面，并加入 ASR 超时后的停止提示。本次未新增付费 Qwen 速度对照，不能据此更新正常生成耗时；真机上传、生成、纠错及失败恢复仍需验证。

## 当前推荐配置

当前 Qwen 体验环境使用以下配置，`0.2.2` 沿用这些配置值：

```env
OPENAI_COMPATIBLE_TIMEOUT_MS=60000
OPENAI_COMPATIBLE_MAX_RETRIES=1
```

`MAX_RETRIES=1` 允许一次 SDK 瞬时失败重试。2026-09-18 续测开始时，实查服务器为 `TIMEOUT_MS=60000`、`MAX_RETRIES=2`；后续部署时同步为已验证的 `1`，减少网络故障时的重试等待。`0.2.2` 仅对流式 ASR 单次请求覆盖为零次自动重试，写作/复核仍遵循上述配置。超时配置值仍保持 60 秒，限制异常等待与正常生成提速分别验收。

## 部署顺序

1. 在 Node `22.23.2`、pnpm `11.19.0` 下运行 `pnpm check` 和 `pnpm build`。
2. 将已验证的 `codex/warm-letter-mvp` 提交部署到香港 API，重启 `warm-letter-api.service`，检查 `/health` 仍为 `demo + WeChat + Qwen profile-match`。
3. 用授权素材完成生成及语音纠错后重生成，记录总耗时与模型错误码；不得把密钥或原始素材写入日志。
4. 在微信开发者工具重新上传小程序，选为体验版，再用任意获授权图片、文字和录音复测：上传、生成、超时离开、首页恢复、编辑确认。

## 后续可选优化

这些项目没有在本轮直接开启，因为会改变安全或供应商边界：

- 用服务端事件推送/长轮询报告真实任务阶段，替代客户端估算进度。
- 为经过授权的媒体做短期对象读取缓存，减少事实复核时的重复读取；缓存必须绑定用户和任务并设置过期清理。
- 评估供应商更快模型或异步批处理，但必须重新完成图片、音频、JSON、来源引用和隐私探针。
- 生产化接入持久化任务队列、共享状态和对象存储；当前单进程内存仓库重启会丢失任务和家书，不能当作生产方案。

## 交接检查

- [x] 公网 API 已部署本轮 commit `395aa8f`，HTTPS 健康检查通过
- [x] `OPENAI_COMPATIBLE_MAX_RETRIES` 实际为 `1`，超时配置为 `60000`
- [x] 小程序 `0.2.2` 已上传，并在版本管理页核实为当前体验版
- [ ] 至少一次真实设备生成耗时和失败/恢复路径有脱敏记录
- [x] 私有转写不进入公开 reader，旧稿发布/朗读保护和失败恢复已验证
- [x] `0.2.2` 流式 ASR 总时限、请求中止、部分转写丢弃及重试路径自动化验证通过
- [x] 已部署 ASR 总时限后端，并上传、核实 `0.2.2` 体验版

此前 `0.2.1` 上传及后台核验记录见 [0.2.1 交接](MINIPROGRAM_0_2_1_DELIVERY_2026-09-19.md)，该版本已由本轮 `0.2.2` 体验版替代。此次后端重启仍受内存仓库限制，旧会话、家书和分享凭据可能失效；真机复测应重新登录并创建新家书。
