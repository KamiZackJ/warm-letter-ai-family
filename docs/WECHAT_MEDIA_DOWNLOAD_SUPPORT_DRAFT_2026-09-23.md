# 微信媒体审核下载失败：支持材料草稿

状态：仅整理证据，尚未发送给微信支持或开发者社区。没有上传原始抓包、用户素材、OpenID、访问令牌或带签名的媒体地址。

更新至：2026-09-24 00:49（北京时间）。以下新增对照均已结束并撤回临时配置，生产继续使用原 LE 证书；媒体下载失败尚未解决。

## 问题

小程序 AppID `wx281b5275e4a1601f` 调用 `media_check_async`（version 2、scene 4）。照片与 MP3 均被受理并返回 trace ID，但认证后的异步回调均返回 `errcode: -1008`。应用依此阻止素材家书分享。

素材地址位于 `https://api.warmjiashu.xyz`，使用 45 分钟有效期的独立签名，不依赖登录 Cookie、不跳转。照片为 JPEG、174,649 字节；录音为 MP3、36,045 字节。相同文件可由自有公网客户端正常 GET，HTTP 200、MIME、长度与文件头正确。

## 已验证

- DNSPod 和阿里公共 DNS 都返回目标服务器 IPv4；无 AAAA 记录。
- 微信加密消息回调可抵达服务器并完成认证，应用返回 HTTP 200；HTTP 200 是回调接收成功，不代表素材审核通过。
- API 使用可信 RSA 2048 位证书。TLS 1.2 客户端限制为 `ECDHE-RSA-AES128-GCM-SHA256`，有 SNI、无 SNI 均通过证书链与域名校验；未关闭证书验证或启用 TLS 1.0/1.1。
- 实际发送的链为 `api.warmjiashu.xyz → YR2 → Root YR（ISRG Root X1 交叉签名）`，OpenSSL 带域名验证到 ISRG Root X1，返回 `0 (ok)`。三张证书签名均为 `sha256WithRSAEncryption`，符合 [Let's Encrypt 默认链](https://letsencrypt.org/certificates/)。并非仅止于 Root YR 的短链。
- 2026-09-23 14:12:26–14:13:26 UTC（北京时间 22:12:26–22:13:26）做过一次有界双向 TCP 443 诊断。303 包、104,404 字节，60 秒完整结束，未触及上限。
- 同时段 12 条相同特征连接带正确 API SNI、声明 TLS 1.2 与 ECDHE-RSA/ECDSA 支持。记录到服务器 ServerHello、选择 `0xc02f` 及后续客户端 RST；没有可确认的明文 TLS Alert，应用没有收到审核素材 HTTP 请求。首轮未分析更早的 FIN，不能由 RST 次序认定客户端收到证书后拒绝。
- 该轮两条失败回调的时间为 14:13:23.951 和 14:13:25.722 UTC，均为 `-1008`。

时间相关性不能单独证明上述每条连接来自微信，现有证据也不足以确定具体证书或网络原因。此前补充 SNI 默认值和切换 RSA 证书都未解决下载失败。

## 完整抓取复核与结论修正

- 15:11:58.759–15:12:58.827 UTC（北京时间 23:11:58.759–23:12:58.827）完整抓取 350 包、148,524 字节，28 条连接均双向；抓取长度 65,535 字节，无截断，未触及 2 MB 上限。
- 12 条声明 56 个密码套件的连接，发送 269 字节 ClientHello 后 0.012–0.185 毫秒即发出 FIN，均早于服务器首个 TLS 数据（ClientHello 后 2.416–3.078 毫秒）。FIN 是客户端提前关闭发送方向，后续 RST 不能证明客户端收到证书后拒绝它。不能据此确认这些连接就是微信媒体下载器，或断言丢包、中间设备故障。
- 完整 ServerKeyExchange 为 `rsa_pkcs1_sha512`、`secp256r1`，公钥 65 字节、签名 256 字节，均在客户端声明支持范围内。另 8 条声明 50 个密码套件的连接采用相同签名、曲线及完整三张证书链，其中 4 条进入双方 ChangeCipherSpec 阶段。原来因抓取截断而未知的签名/曲线已经补齐，未发现可支持 PSS 不兼容推断的证据。
- 本轮确认操作的客户端返回时间为 15:12:25.852 UTC；两条审核记录在 15:12:34.312 和 15:12:36.877 UTC 更新为 `failed/-1008`。15:12:34.311、15:12:36.875 和 15:12:40.540 UTC 收到认证回调并返回 200，包含重复回调；所有 HTTP 方法的审核素材请求仍为 0。

此前把失败归到“收到握手回复后、HTTP 前”的推断已撤回。当前只确认媒体下载仍失败且素材 HTTP 请求未到达应用，实际失败阶段和根因待查。后续证书与 TLS 实现对照依据下述独立控制结果进行，不把抓包中的未知连接身份或 RST 当作已知根因。

## 9 月 24 日补充对照

下表统一采用北京时间（UTC+8）。三次公开图片对照使用独立诊断记录，不关联家书、不放行真实素材；记录和临时静态路由均已清理。

| 对照 | 提交时间 | 已认证的回调时间与结果 | 条件与证据边界 |
| --- | --- | --- | --- |
| 阿里云 OSS 公开 JPEG，HTTPS | 09-23 23:40:57.390 | 23:41:00.965，`pass` | 496,395 字节，无鉴权与查询参数。证明同一测试账号当时可提交审核、微信可下载该 OSS 图片并回传结果。 |
| API 同主机公开 PNG，HTTPS | 09-23 23:46:21.746 | 23:46:25.495，`failed/-1008` | 8,287 字节，无查询参数，有 `.png` 后缀，严格 HTTPS 自检 200。Caddy/TLS 未改变。 |
| API 同主机、相同 PNG 字节，HTTP | 09-24 00:04:22.623 | 00:04:27.484，`pass` | 精确临时路径，无重定向；其他 HTTP 请求仍跳转 HTTPS。与上一轮的随机路径标识不同，静态处理及文件相同。 |

同主机 HTTPS 失败、HTTP 成功将排查重点指向 HTTPS 访问链路，但不能单独证明 CA、证书算法、服务端实现或平台域名策略是原因。不能仅将失败归于签名查询、文件体积或缺少扩展名。账号、真实素材、微信令牌及审核 API 未改用 HTTP。

### 更换真实证书链仍失败

09-24 00:31 独立预签发取得 ZeroSSL RSA DV SSL CA 2 候选证书，已验证精确域名、系统信任链、RSA 2048 及公私钥匹配。00:32 短时切换后，以新 TLS 连接核对 SNI／无 SNI、TLS 1.2／1.3 均实际返回候选证书指纹；不是只修改配置却继续使用缓存中的原 LE 证书。

现有真实照片和 MP3 分别于 00:32:52.941、00:32:53.305 重新提交，00:32:57.820、00:32:57.944 的实际回调仍为 `failed/-1008`。更换 CA 没有解决该次下载失败，不能仅归因于原 LE 链。随后恢复原 LE 配置及自动证书管理；没有重启 API、重传素材或重新生成家书，失败记录未放行。

### 更换 HTTPS 服务实现仍失败

09-24 00:49 临时由官方 Ubuntu Nginx/OpenSSL 在同一公网 443 接收 API 流量，继续使用原 LE 证书，仅启用 TLS 1.2／1.3 和现代 AEAD 套件，再通过严格校验的 HTTPS 回代理原 Caddy。Windows 公网新连接的 `/health` 响应头确认确实进入该对照路径。

真实照片和 MP3 分别于 00:49:26.323、00:49:26.714 提交，00:49:29.865、00:49:30.798 的回调仍为 `failed/-1008`。对照访问日志仅见确认 POST 409 和两条回调 POST 200，没有素材 GET／HEAD。warn 级错误日志为空，不能据此排除只在 info 级别记录的握手中止。

临时 NAT 规则及 Nginx 已撤回，原 Caddy 配置、API 与数据库保持原状，公网健康正常。当前结果不足以认定 Caddy 是唯一原因，也不能直接断言微信封禁了该域名。证书切换和服务实现切换都没有提供下载成功的证据，下一步需要平台下载侧日志定位具体失败阶段。

## 请求平台协助核查的内容

请按上述提交及回调时间，关联媒体下载任务的服务端日志，说明 `-1008` 发生的具体阶段和底层错误：

- DNS：是否实际解析到目标 IPv4、是否存在解析失败或与公共 DNS 不同的结果。
- TCP：是否向目标 443 建连，是否发生连接超时、拒绝、重置或主动取消。
- TLS：是否发送 SNI、实际协商版本及套件、是否完成握手；如失败，请提供证书校验错误、TLS Alert 或内部错误分类。
- HTTP／下载：是否发出 GET／HEAD、是否收到状态码或响应体，以及下载超时、长度、格式或地址策略检查中的具体失败项。

如平台对证书链、签名算法、素材 URL 格式或域名访问存在额外限制，请提供适用规则及本次命中的依据。自有公网下载成功、回调抵达以及不同 TLS 对照都不能代替平台下载侧的实际记录；本草稿不预设根因。

如需 trace ID，应先确认是微信官方私密支持渠道，再按需提供最少标识。不要公开访问令牌、OpenID、完整素材签名 URL、原始媒体或网络抓取。无需重复上传素材、重新调用 AI 生成，或将失败任务手动改成通过。

当前发布状态及后续实测以[正式发布记录](./PRODUCTION_RELEASE_1_0_0_2026-09-23.md)为准；审核接入实现与诊断边界见[内容安全说明](./WECHAT_CONTENT_SAFETY.md)。
