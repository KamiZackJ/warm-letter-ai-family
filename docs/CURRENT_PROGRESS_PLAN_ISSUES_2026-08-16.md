# 暖笺当前进度、推进计划与问题清单

- 更新时间：2026-08-16 18:25（Asia/Shanghai）
- 仓库：`KamiZackJ/warm-letter-ai-family`
- 分支：`codex/warm-letter-mvp`
- 远端已发布基线：`7b90d2c67f3fa18f27ab7a222f202f90a7eb9fb0`
- 草稿 PR：[PR #1](https://github.com/KamiZackJ/warm-letter-ai-family/pull/1)
- 文档性质：项目经理阶段快照。只有已提交、已推送且对应远端 CI 成功的内容才计为“已发布”；工作树中的实现统一计为“进行中”。

## 1. 阶段结论

暖笺当前已具备可运行的 Fastify API、React H5 和微信小程序开发/演示骨架，`G0` 内部开发门禁已通过，`W2` 本地/单实例分享安全已有基线。远端 `7b90d2c` 的 push 与 pull request 两条 GitHub Actions 均成功：

- [push run 31939946613](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/31939946613)
- [pull_request run 31939948768](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/31939948768)

除本次文档更新外，当前工作树还有一批 H5 字号、小程序异常恢复、列表错误传播和公开回复幂等改动，共 18 个修改文件、6 个未跟踪文件，尚未暂存、提交或推送，也未进入远端 CI。本轮定向验证已发现 1 个小程序收信页测试断言仍未适配新的幂等键参数，因此该批次不能标记为完成。

真实 OpenAI 四素材闭环、微信开发者工具/双真机闭环、正式内容审核、完整删除生命周期、生产持久化与监控、长图短片和比赛提交材料仍未关闭。当前不得宣称达到 `G1`、`G2`、MVP、公测、生产或比赛提交候选。

## 2. 已发布并有远端证据的基线

| 范围 | 当前事实 | 证据边界 |
| --- | --- | --- |
| API | 已提交上传首次写入防覆盖、公开分享安全和文件系统对象/metadata 原子发布；已记录 API `123/123` 本地基线 | 仍是本地文件系统适配器，不代表生产对象存储、真实 AI 或跨实例一致性通过 |
| H5 | 已提交移动端家书正文 20px、分享阅读、媒体恢复和适老基础返修；已记录 Web `38/38` 本地基线 | 不包含当前工作树中的三档字号控件 |
| 小程序 | 已提交环境隔离、上传头部白名单、删除确认、ARIA 和触控热区返修；已记录小程序 `30/30` 本地基线 | 不包含当前工作树中的首页/素材/收信异常恢复批次，也不替代微信运行态验收 |
| contracts | 已记录 `11/11` 本地基线 | 共享 DTO/schema 尚未覆盖全部三端私有模型 |
| GitHub | 远端 HEAD `7b90d2c`，两条 `verify` 均成功 | 只覆盖已推送内容，不覆盖当前未提交工作树 |

## 3. 当前工作树进度

| 工作流 | 已实现或已验证 | 当前状态与剩余工作 |
| --- | --- | --- |
| H5 阅读字号 | 已实现 `20/24/28px` 三档字号、偏好持久化、`aria-pressed`、键盘/焦点和 44px 控件；Web `51/51`、typecheck、Vite test 构建和 Web Interface Guidelines 静态复核通过 | 进行中；待完成 320/390/768/1440、200% 缩放、刷新后偏好保持、无横向溢出、截图/console 浏览器矩阵，以及最终差异复核、证据索引和独立提交 |
| API 公开回复幂等 | 已读取 `idempotency-key`，同键同内容重放原回复，同键不同内容返回 `409 IDEMPOTENCY_KEY_REUSED`；API 全量 `124/124`、`public-access.test.ts` `12/12` 和 API typecheck 通过 | 进行中；待并发/上限边界差异复核和独立提交 |
| 小程序列表错误传播 | `listLetters()` 只忽略明确的 `404 + LETTER_NOT_FOUND`；网络、鉴权、429、5xx 和其他 404 向上抛出；定向 `8/8` 通过 | 进行中；待全量小程序回归和真实 API 链路验证 |
| 小程序首页/素材页 | 已实现旧数据保留、持久错误、就地重试、最新请求获胜、分阶段批量上传和多失败上下文；本轮首页 `4/4`、素材 `13/13` 通过 | 进行中；素材页差异较大，必须独立审查幂等、远端成功/本地持久化失败和卸载边界 |
| 小程序收信页 | 已实现图片尝试代次、音频上下文/来源代次、媒体刷新与回复合并、回复草稿保留、禁用态和卸载保护 | 阻塞于测试收口；当前收信页 `7/8` 通过，失败断言仍按旧三参数调用，实际已新增第 4 个幂等键参数 |
| 小程序回复 API | POST 直接返回 `Reply`，发送幂等键，不再在成功 POST 后追加一次可能失败的 reader GET；定向 `1/1` 通过 | 进行中；需确认重试复用同一键，并补编辑中输入、媒体刷新竞争和卸载场景 |

本轮小程序全量结果为 `61/62`，其中相关定向集合为 `33/34`，小程序 typecheck 通过。唯一失败项位于 `reader-page.test.ts` 的回复重试断言：实现调用为 `letterId + content + shareToken + idempotencyKey`，测试仍只断言前三个参数。该失败目前更像测试未同步，但在修正并补齐竞争/卸载覆盖前不能据此认定实现正确。

## 4. 推进计划

### P0：收口当前未提交批次

1. 修正收信页回复重试测试，明确断言失败后重试复用同一个幂等键。
2. 补齐收信页四类关键覆盖：请求期间继续编辑不丢输入、媒体刷新与回复并发不覆盖、旧音频回调不影响新来源、页面卸载后异步结果不再写状态。
3. 复跑小程序定向测试、全量测试和 typecheck；任何失败先留在本地，不进入提交。
4. 为 Web 回复补同等幂等语义：失败重试保留请求键、发送 `idempotency-key`、提交期间禁用输入，并补对应测试。
5. 补 H5 真实浏览器矩阵，再复跑 API/Web/小程序/contracts 全量测试、根级 `pnpm check`、`pnpm build` 和 Web production bundle verifier。
6. 对素材页和收信页做独立差异审查，确认没有旧数据覆盖、重复远端操作、卸载后写状态或错误上下文丢失。
7. 按“API 回复幂等”“小程序异常恢复”“H5 字号”拆分提交，逐批推送；每批必须等待 push 与 pull request 两条 CI 成功。
8. CI 全绿后同步 `STATUS_UPDATE_2026-08-16.md`、`PROJECT_STATUS.md`、`ACCEPTANCE_CHECKLIST.md` 和证据索引中的最新 HEAD、测试数与未关闭门禁。

### P1：补齐外部验收

1. 使用授权照片、截图、中文语音和文字完成真实 OpenAI 结构化草稿闭环，保存脱敏 request ID、模型、usage、耗时、失败和重试证据。
2. 使用正式微信 AppID、`code2Session`、可达 HTTPS API、体验成员和两台设备完成上传、预览、播放、删除、分享、回复、重签及寄信端回查录像。
3. 落地正式内容审核、完整删除 SLA、生产数据库/对象存储、共享限流、队列、监控和失败补偿。
4. 从同一确认版本产出手书长图和 30 秒以上家书短片，完成许可台账、失败重试和一致性验证。
5. 完成种子家庭观察测试、视频/PDF、AIGC 声明、双话题、报名字段、资格与学校/主体证据归档。

## 5. 当前问题与风险

### 代码与验证

- 当前工作树混合了 24 个路径的多条工作流，差异较大；必须拆分审查和提交，不能一次性暂存全部文件。
- 小程序定向测试尚有 `1` 个失败，远端 CI 也完全未覆盖当前批次。
- Web 端公开回复尚未接入与 API/小程序一致的幂等键重试语义。
- “重新生成”并发保护、完整历史家书入口和跨设备历史/署名仍是独立 P1，不能被当前恢复批次顺带视为完成。
- `PROJECT_STATUS.md`、`ACCEPTANCE_CHECKLIST.md` 等旧文档仍可能引用旧 HEAD 或旧 CI；完成当前批次前以本文件和 `STATUS_UPDATE_2026-08-16.md` 的已发布基线为准。

### 外部阻塞

- 缺真实 OpenAI 凭据、模型权限、额度、授权四素材和人工事实基准。
- 缺正式微信 AppID、可达 HTTPS API、体验成员、开发者工具/双真机证据。
- 正式内容审核、完整删除、生产持久化、共享限流、监控和备份策略未闭环。
- 参赛资格、目标期次、学校截止、快手账号、小程序主体、视频、PDF 和报名回执尚未完整归档。

## 6. 项目管理与推进规则

- 项目经理每个阶段只更新三类事实：已完成证据、新增问题、下一项可执行工作。
- 任一实现只有在“测试通过 -> 独立复核 -> 提交 -> 推送 -> 最新远端 CI 全绿”后才从“进行中”改为“已发布”。
- 当前推进顺序固定为：收信页测试收口 -> Web 回复幂等 -> 全量门禁 -> 差异审查 -> 拆分提交 -> 远端 CI -> 外部验收。
- 下一次阶段汇报触发条件：小程序全量转绿、Web 回复幂等完成、至少一个独立提交推送且两条 CI 成功，或出现新的 P0 阻塞。

## 7. 临时文件与证据约束

- 固定运行时：Node.js `22.23.2`、pnpm `11.19.0`。
- `TEMP`、`TMP`、`TMPDIR`、`WARM_LETTER_TMP_DIR` 统一指向 `D:\tmp\warm-letter-ai-family`。
- 测试、构建、浏览器会话、截图、缓存和候选副本不得在 C 盘新建任务临时目录。
- 不提交 API key、访问令牌、原始家庭素材、签名 URL、个人信息截图或未脱敏日志。
