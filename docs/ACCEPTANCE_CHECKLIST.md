# 暖笺评审、验收与返修清单

本清单是阶段放行依据。所有项目默认未通过；勾选时必须在同一行或对应验收记录中填写可复核证据。`代码已写`、`本机看起来正常`、`视频中演示过`均不能单独作为完成证明。

> 历史快照说明：早期实现基线 `f60e759a925e0eb4189afa23344b2489e9bb632f` 的 contracts `17/17`、Web `70/70`、小程序 `125/125`、API `135/135` 和远端 CI 结果仍作为当时证据保留。当前工作树的 355 项本地回归、CASE-001 受控真实素材包和最新门禁请以 [`CURRENT_HANDOFF_STATUS_2026-08-28.md`](./CURRENT_HANDOFF_STATUS_2026-08-28.md) 为准；真实 AI、正式微信环境、双真机、生产持久化/审核和真人测试仍未关闭，因此不得放行 G1、G2、MVP、公测、生产或比赛提交。

## 1. 验收记录格式

每次评审记录：版本/提交 SHA、环境、验收人、日期、测试数据、步骤、预期、实际、截图/录像/日志、结论和返修单 ID。失败项进入 `PROJECT_STATUS.md` 返修队列，修复后由非原实现者或项目经理复测。

放行级别：

- `G0 内部开发`：允许 fake AI、模拟存储和本地数据。
- `G1 内部演示`：完整闭环可重复运行，异常路径基本可恢复。
- `G2 用户测试`：使用真实 AI、授权素材和稳定部署，隐私与长辈体验可验证。
- `G3 提交候选`：比赛视频、PDF、Demo 和代码版本一致，所有 P0/P1 关闭。
- `G4 已提交冻结`：公开链接、回执和校验值已归档，不再修改已提交作品。

编号说明：本文件的 `G0-G4` 是发布放行级别；`PROJECT_STATUS.md` 的日期里程碑使用 `M0-M7`；`B1-B4` 是核心闭环证据门禁，`W1-W4` 是执行波次。

`M0-M7` 的唯一含义固定为：`M0` 立项与赛规基线、`M1` 可运行骨架、`M2` 真实 AI 核心闭环、`M3` 动态家书与回复、`M4` 长图与短片、`M5` 用户测试与质量收敛、`M6` 比赛材料与发布、`M7` 上架与决赛准备。其他文档和排期不得另行重定义这些编号。

### 1.1 当前实施批次验收顺序

以下门禁可以并行建设和复测，但进入 G2 用户测试前必须全部通过；项目经理只接受可复核证据：

**B1 真实媒体上传**

- [x] API 使用真实 HTTP 二进制 PUT/GET 和文件系统对象，不再以 `memory://` 或仅登记对象键代替传输；小程序真实模式已接 `presign -> upload bytes -> complete`。`presign`/`complete` 使用 API Bearer，上传 PUT 只发送短期、绑定素材与 MIME 的 `x-warm-letter-upload-token`，不得携带登录 Bearer 或 Cookie。
- [x] 上传对象采用原子首次创建；顺序重复或并发 PUT 只有一次成功，其余返回 `409 UPLOAD_ALREADY_RECEIVED`，首次字节不被覆盖。实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 的 API `114/114` 通过。
- [x] 服务端在 complete 前验证对象存在、owner、MIME、扩展名、大小和文件魔数；失败对象不进入 `READY`。
- [x] owner 可受控读取；未上传、越权、超限、MIME/扩展名/魔数错误和删除后的对象均被拒绝，删除会物理清理对象和 sidecar。
- [x] 日志去除查询串并脱敏 `Authorization` 与 `x-warm-letter-upload-token`；文件系统错误不暴露绝对路径/对象键。历史 W1 本地代码基线 `7f5421b` 的 Node 22 干净回归为 API 54/54、contracts 11/11、小程序 8/8，类型检查和构建通过；当前 FIX-015 工作树结果见 `PROGRESS_2026-08-16.md`。
- [ ] 微信开发者工具/真机使用本地图片和语音完成实际上传、读取与删除；公开短期媒体签名在 B3/B4 验收。

**B2 多模态 AI**

- [x] OpenAI provider 从真实对象读取图片并构造 image input，音频先转写；缺失对象会拒绝生成，provider 专项 15/15 通过。
- [x] generation job 的 HTTP 响应采用显式公共白名单，不返回 `userId` 或 provider 内部 message；超时/非法输出、404 等价、幂等重放和非法幂等键 5/5 通过。
- [x] 小程序在 POST 202 响应丢失后复用持久化幂等键；轮询短暂断网继续同一 job，stale job 单次调用最多重启一次；恢复与轮询 8/8 通过。
- [ ] 使用真实供应商凭据让授权图片/截图/语音进入视觉/OCR、转写或音频理解，不以 mock client 结果代替端到端证据。
- [ ] 固定样例中的图片细节、截图文字和语音原句可在结构化草稿中核对，并分别关联正确素材 ID。
- [ ] 真实供应商调用、schema 失败、非法来源引用、超时、重试和用户重生成均有证据；凭据只来自环境变量。
- [ ] 未选择、已删除或不属于当前用户的媒体不会发送给 AI。

**B3 短期可撤销分享**

- [x] 分享 token 有 TTL，服务端仅存 hash；重新签发会撤销旧 token，并支持显式撤销。
- [x] 有效 token 可读信/回复；过期读取、撤销读取/回复和猜测 token 已有 4xx 测试。
- [x] 缺失、未知、错家书、错素材、非规范编码、过期、撤销 token 对 reader/media/reply 的完整 HTTP 矩阵通过；公开不存在统一 404，撤销/过期统一 410。
- [x] API 为非文本来源返回独立 HMAC `mediaUrl/mediaExpiresAt`；媒体凭据绑定 share/letter/material，默认 5 分钟且不超过父分享期限，重发/撤销后旧媒体 URL 返回 410。
- [x] 媒体地址不复用整份分享 token；严格拒绝非规范 Base64URL 同字节别名，生产支持多密钥轮换。
- [x] 公开响应字段白名单、日志脱敏、reader/media/reply 双桶限流、`Retry-After`、正文/称呼安全、超时失败关闭和 100 条并发原子上限均有测试。
- [x] 已确认/已发布正文不可漂移；重签/撤销只修改分享记录，撤销前后 owner 私有 `confirmedDraft` 完全一致。
- [x] 本地实现 `fc994bd` 对改写段落强制四态来源归因：遗留引用会在改写后清空并变为待核对，确认发布拒绝待核对/伪造 AI/不一致归因；写信人可重选当前素材或标记本人补充，小程序/H5 Reader 公开相应披露。干净 D 盘回归为小程序 `121/121`、API `132/132`、contracts `13/13`、Web `69/69`，待推送后由 PR 最新 HEAD 双 CI 和微信真机复核。

**B4 公开来源与跨设备回复**

- [x] strict 公共来源契约只允许脱敏字段并拒绝 `userId/objectKey`；客户端已预留 `mediaUrl/mediaExpiresAt`。
- [x] 小程序公开页不再读取寄信人的私有素材或 `getLetter`，回复后使用同一分享 token 重读 reader。
- [x] API 返回独立且更短期的 `mediaUrl`，通过真实字节读取、自然到期、重签/撤销后 410、规范编码和密钥轮换测试。
- [x] H5 已实现消费 API `mediaUrl`、图片/原始语音控件、过期/失败/重试状态，并在回复后用同一 token 重读 reader；类型检查和构建通过。
- [x] H5 真实浏览器在 390x844、412x915、1440x900 使用 API 地址加载两张图片和原始 WAV；媒体失败后重试恢复、系统朗读、回复后同 token 刷新均通过主代理人工交互验收，尚未形成可提交自动请求时间线。
- [x] 重签后旧 reader 与 photo/screenshot/audio 三条旧媒体 URL 均返回 410，旧 H5 显示停止分享；新 token 的 reader 与三媒体均返回 200，原回复仍可见。
- [x] FIX-015 H5 补充浏览器验收已覆盖 320/390/768/1440 px、200% 字体、最长中文和无媒体状态，均无横向溢出，console error/warning 为 0。完成结论记录于 `d28c9d9` 并包含在历史 HEAD `71d6aae`；原始截图、console 和页面状态仍只位于 `D:\tmp\warm-letter-ai-family`，尚未持久归档，不能作为 G3 冻结证据。
- [ ] 小程序使用 API 返回的地址完成真机图片预览和语音播放；H5 篡改媒体凭据的 UI 展示仍需单独回归，不能由 API 负向测试代替。
- [ ] 清空本地存储的独立设备可打开、预览图片、播放语音、发送回复并用同一 token 刷新成功状态。
- [ ] 寄信端可回查回复；两个账号/设备看到的确认正文、来源与回复一致，错误 token 被拒绝。

**批次退出**

- [ ] B1-B4 全部通过自动测试和一次脱敏双设备端到端验收；证据记录版本、环境、步骤、结果和录像/日志。
- [x] 最终代码基线 `d1c525db521ac857866c019ac9dc264396c006e3` 复核通过：contracts 10/10、API 40/40、`public-access` 11/11，四工作区类型检查和构建全绿；非规范媒体别名隔离测试连续 12 次通过，合法媒体凭据遇对象缺失返回 410 且不泄露对象键或存储错误码。
- [x] 本轮测试/构建仅在进程级使用 `D:\tmp\warm-letter-ai-family`；未修改系统/用户全局环境变量。
- [ ] 并行验收任务结束后清理 `D:\tmp\warm-letter-ai-family` 中本项目产生的临时文件，并复核没有回落到 C 盘任务目录。
- [x] 三张 H5 截图、SHA-256 与脱敏交互结论已归档到 [`docs/evidence/2026-08-15/H5_VALIDATION.md`](./evidence/2026-08-15/H5_VALIDATION.md)，并绑定代码基线；含原始 owner/share token 的 `e2e-state.json` 未进入 Git。
- [ ] 生产配置不能自动回退 mock/Demo，且 B1-B4 完整双设备证据通过。
- [ ] 本批通过只允许进入 G2 用户测试评审，不代表微信鉴权、删除、内容安全、比赛材料或上架已完成。

### 1.2 下一阶段验收安排

| 波次 | 主责角色 | 必须证据 | 退出条件 |
| --- | --- | --- | --- |
| W1 B2 真实 AI | 队长；成员一提供授权脱敏素材；项目经理复核 | 真实供应商请求/响应、照片细节、截图 OCR、语音原句、正确素材 ID、超时/重试 | B2 所有未勾选项关闭，mock client 不计证据 |
| W2 B3 分享安全 | 队长；两名项目经理；独立安全 QA | 代码 SHA、独立媒体凭据、完整 404/410 矩阵、429、正文/称呼安全、并发上限、日志脱敏、浏览器到期/续签 | 本地/单实例 B3 已全绿并绑定最终代码 `d1c525d`；浏览器证据绑定 `623811b`；生产正式审核 P0、DB/共享限流/trustProxy P1 继续开放 |
| W3 B1/B4 双设备 | 队长；成员一/成员二分别操作两端；项目经理验收 | 微信开发者工具和真机录像，实际上传、预览、播放、回复、刷新、寄信端回查 | B1 真机项和 B4 全部关闭，H5 截图不替代小程序/真机证据 |
| W4 批次证据检查点 | 项目经理 | 源码 SHA、全仓检查/构建/UTF-8、B1-B4 证据索引、生产不回退 mock 证明 | 只确认当前证据包完整性；W4 本身不批准 G2、MVP、公测或生产发布 |

任一影响里程碑的 P0 阻塞超过 4 个工作小时，项目经理当日升级队长并记录替代推进项；同一项复测失败两次后必须调整实现方案或缩减范围，不得改低验收标准。

## 2. 赛规与外部依赖门禁

- [ ] 团队报名“AI产品实现”，没有同时报名另一方向。证据：报名预览/回执。
- [ ] 全体成员是在校生，团队不超过 5 人且含队长 1 人。证据：平台团队页；敏感证明离线保管。
- [ ] 每名成员只参加一支团队；暖笺只提交一次且不跨月重复参赛。证据：逐人确认和报名历史检查。
- [ ] 指导老师不超过 2 人，并已确认学校内部认定要求。证据：团队页/学校通知。
- [ ] 已确定目标期次，默认 09-30，最终截止不得晚于 2026-10-31 23:59。证据：项目决策记录。
- [ ] 快手账号、小程序主体/AppID、真实 AI/部署凭据和素材授权均有负责人及可用证据。

## 3. MVP 核心闭环

- [ ] 新用户能在 3 分钟内完成：进入产品 -> 添加至少一种素材 -> 填写意图/风格 -> AI 生成 -> 编辑 -> 确认 -> 阅读。
- [ ] 照片、聊天截图、语音、文字、日程截图五类主动素材有明确入口、类型/大小限制和错误提示。
- [ ] 用户可逐项查看和删除素材；删除后生成任务不能继续读取，公开阅读端看不到原始素材和内部存储键。
- [ ] 家书设置至少包含收信人、关系/语气、篇幅、重点和禁用内容，并真实影响生成结果。
- [ ] 生成任务具备排队、运行、成功、失败、超时和重试状态；重复请求不会产生重复业务结果。
- [ ] 草稿可逐段编辑、删除和重生成；每个事实段保留属于当前家书素材集合的来源引用。
- [ ] 用户改写段落后明确标记“用户修改”，可预览、删除或重选来源；确认前检测正文与引用不一致，不自动沿用错误来源。
- [ ] 用户确认指定草稿版本后正文冻结；未确认内容绝不会出现在阅读页。
- [ ] 确认快照包含标题、问候、正文、结尾、署名和媒体关联；换设备后小程序、H5、长图和短片读取一致。实现基线 `f60e759` 已通过契约/API 快照、小程序无本地署名缓存读取和 H5 展示自动化；长图/短片与微信双真机尚未验，故本项不勾选。
- [ ] 已发布家书不可被后台重生成漂移；修改需创建新草稿/新版本。
- [ ] 长辈端支持大字阅读、明显的语音播放入口和不超过 240 字的简单回复；回复可被寄信人回查。
- [ ] H5 使用短期签名令牌读取确认版本；过期、篡改、越权和不存在的令牌返回安全且可理解的失败状态。
- [ ] 收信人在全新设备、未登录且无寄信人本地缓存时，仍能使用分享 URL 中的 token 阅读和回复；回复请求显式携带当前页面 token。
- [ ] 公共阅读 DTO 提供最小化脱敏来源和短期签名媒体地址；收信端不读取寄信人的私有素材接口，两账号/两设备显示一致。

核心闭环证据包：全程录屏、网络请求/任务状态的脱敏记录、数据库或内存仓库状态快照、确认前后对比、回复回查截图。

## 4. AI 核心价值与生成质量

- [ ] 正式演示版本调用真实 AI；fake provider 只存在于开发和确定性测试环境。
- [ ] 演示、测试、生产配置强隔离；生产构建不能自动回退 mock/样例数据，演示环境具有永久可见标识。
- [ ] AI 输入只包含用户为当前家书确认的素材和设置，不读取未选数据。
- [ ] AI 能从至少两种不同模态提取事实，并输出通过共享 schema 校验的结构化草稿。
- [ ] 每个事实段可追溯到素材 ID；无来源的抒情/问候内容有清晰类型边界，不伪装为事实。
- [ ] 使用 10 组固定样例评估事实一致性、遗漏、夸大、敏感信息和收信人适配；严重事实编造为 0。
- [ ] 提供一次 AI 超时、格式错误或供应商失败的可恢复演示，不泄露提示词、令牌或素材原文。
- [ ] 视频和 PDF 能用一句话解释 AI 的不可替代价值：把多模态生活碎片转成可追溯、适合特定家人的结构化表达。

## 5. 隐私、安全与内容合规

- [ ] 不直接读取微信聊天记录、不扫描完整相册、不克隆亲友声音；权限文案与实际行为一致。
- [ ] 素材、家书、任务和回复均校验 owner；登录令牌和公开阅读令牌用途分离且短期有效。
- [ ] 上传文件校验 MIME、扩展名、大小和归属；不可信文件不会直接公开或执行。
- [ ] 日志不记录素材原文、访问令牌、签名 URL、微信 openid、完整 AI 提示词或隐私字段。
- [ ] 删除、过期和对象存储清理有可测试行为；演示账号可执行素材删除。
- [ ] 账号/全量数据删除可查询状态，分享立即撤销，数据库/对象存储/缓存/队列按 SLA 清理，备份与第三方保留规则已披露；至少完成 3 次删除演练。
- [ ] 用户确认界面明确提示 AI 生成、来源和发送责任，允许删除敏感段落和禁用内容。
- [x] 本地公开回复的正文和称呼均通过可注入、失败关闭的违法违规/低俗/暴力/歧视/攻击性兜底检查；指定绕过样例 422 且不落库。
- [ ] 生产接入正式内容审核供应商，并提供用户修正、申诉和人工处理入口；本地关键词规则不能替代该门禁。
- [ ] 演示素材、人物肖像、语音、音乐、字体、图标和代码依赖均进入授权台账；不含明星脸和未授权 IP。

## 6. 中文、可访问性与视觉质量

- [x] `packages/contracts`、`apps/api`、`apps/miniprogram`、`apps/web`、README 和所有 `docs` 的目标范围共 82 个文本文件通过严格 UTF-8 字节解码，且不存在替换字符、私用区字符或 mojibake 特征。
- [x] UTF-8 自动搜索零命中；H5、API 错误信息和测试夹具已抽查，小程序页面仍保留开发者工具/真机视觉复测。
- [ ] 小程序在常见手机宽度下无文字重叠、按钮截断、横向滚动或不可点击区域，最长中文文案可换行；H5 的 320/390/768/1440、200% 字体、最长中文和无媒体矩阵已完成，但不替代小程序真机复测。
- [ ] 阅读端大字号、对比度、焦点/按压反馈、加载/空/失败/禁用状态完整；图标按钮有可理解标签。
- [ ] 主操作触控区不小于 44x44 px；H5 普通正文不小于 16 px、长辈家书正文默认不小于 20 px、来源/辅助文字不小于 14 px；标准/大字/特大字模式下均无溢出。
- [x] 本地代码尺寸与适老返修已通过：H5 来源标签 14px/28px、媒体按钮 44px、移动端家书正文 20px、次级文字对比度和 disclosure ARIA；小程序补持久错误/重试/返回、删除确认/取消、输入 ARIA、分段 `aria-pressed`、辅助字号缩放及至少 88rpx 触控热区。Web `38/38`、小程序 `30/30`、typecheck/build 通过。
- [ ] 上述字号与触控调整需在微信开发者工具和真机完成视觉、点击和最长中文文案复测后，才能关闭 `FIX-023`。
- [x] H5 真实浏览器已覆盖 320/390/768/1440 px、200% 字体、最长中文和无媒体状态；本轮移动端正文 20px 在 320/390 宽度下再次通过计算字号、非空像素、横向溢出和 console 检查，截图与哈希见 [`UI_ACCESSIBILITY_RETEST.md`](./evidence/2026-08-16/UI_ACCESSIBILITY_RETEST.md)。该结论不关闭小程序视觉门禁。
- [ ] 将完整 H5 原始会话、console 和页面状态从 `D:\tmp\warm-letter-ai-family` 形成最终索引；当前持久证据已绑定实现提交 `18027406f7d0683526e7664a41f27625e5a97a18`，H5 篡改媒体凭据 UI 仍需单独回归。
- [ ] 接入 Figma 前，颜色、字号、间距、按钮、表单、来源标记和状态组件有统一设计令牌/规则；接入后对齐设计稿并记录偏差。
- [ ] 至少 3 名年轻用户和 3 名长辈完成观察测试；记录完成时间、卡点、误解和是否需要帮助。

## 7. 动态家书、长图与短片

- [ ] 动态家书只读取已确认版本，图文、语音/BGM、来源和回复在小程序/H5 中一致。
- [x] H5 真实浏览器已区分系统朗读和原始 WAV，验证朗读控制、原始音频控件以及媒体加载失败后的重试恢复。
- [ ] 小程序原始语音/合规 TTS、自动播放策略和无音频状态仍需真机验证；H5 自然到期/续签已完成，篡改媒体凭据的 H5 UI 展示仍需回归。
- [ ] 手书长图从同一确认版本生成，包含标题、日期、正文、照片、署名和可选回链；至少在 1080 px 宽和打印预览中无溢出、裁切或乱码。
- [ ] 家书短片从同一确认版本生成，照片、文字、语音和合规音乐时间轴同步；失败任务可重试且不会生成重复成品。
- [ ] 动态页、长图、家书短片的正文内容一致；任何差异均来自版式缩略并有规则记录，不能重新生成另一份事实正文。
- [ ] 输出媒体中使用的 AI 工具、TTS、音乐、字体和图片许可均可追溯，片尾/简介按要求标注。

## 8. 工程质量与回归

- [ ] `pnpm install` 可复现，Node/pnpm 版本与文档一致，环境变量模板不含真实密钥。
- [x] 本轮 Windows 验收命令均使用进程级 `$env:TEMP`/`$env:TMP` 指向 `D:\tmp\warm-letter-ai-family`；没有运行 `setx`、注册表或系统设置修改全局临时目录。
- [x] 最终代码基线 `d1c525db521ac857866c019ac9dc264396c006e3` 的 `pnpm check`、`pnpm build` 全部通过：contracts 10/10、API 40/40、四工作区构建全绿；结果已写入脱敏证据记录。
- [x] 本地验证实现提交 `18027406f7d0683526e7664a41f27625e5a97a18` 全量回归通过：API `114/114`、Web `38/38`、小程序 `30/30`、contracts `11/11`；其后续提交已由当前 PR 最新 HEAD `09f03c159900f9ad329b390b618d99800968d9f9` 的两条远端 CI 复核通过。
- [ ] 文件系统对象独占创建后、metadata sidecar 完成前发生进程级强制终止时，可通过原子提交、恢复扫描或等价补偿避免长期遗留孤儿对象或不完整 sidecar；该项为生产 P1，不重新打开重复 PUT 的本地 P0。
- [ ] 正式测试集固化对象创建后各中断点的故障注入与清理回归，证明失败对象不可见、可恢复且不会长期占用存储。
- [x] CORS 使用明确来源白名单；已测试允许配置来源且拒绝未配置来源，真实 H5 分享 URL 可跨域读取和回复。
- [x] 真实 HTTP listener 已覆盖请求体/content type 边界：错误空 JSON 保留 400，正常无请求体 POST 返回 202，框架 4xx 不误映射为 500。
- [x] 有效分享 token 可读取确认版本并提交回复；真实 H5 已完成跨域加载、API 图片/原始语音、媒体故障恢复、系统朗读、回复送达及同 token 刷新。
- [x] 重签浏览器验收已完成：旧 reader、旧媒体和旧 reply POST 均为 410；新 reader/媒体均为 200，新页无 alert/横向溢出，两图 natural 800x600，audio `readyState=4`，原回复仍可见。
- [x] 重签、显式撤销、缺失、未知、错家书、错素材、分享过期和媒体过期的 reader/media/reply HTTP 矩阵已由表驱动测试覆盖。
- [x] 缺失、未知、错绑定和非规范公开凭据统一返回 404；分享撤销/过期与媒体自身过期返回对应 410，不泄露家书存在性。
- [x] 错误、过期或已撤销 token 的 reply POST 被拒绝；违规正文/称呼 422、审核不可用 503，所有失败均不落库。
- [ ] 共享 contracts 的 schema、状态机和错误结构由 API、小程序和 H5 复用，没有重复且漂移的 DTO。
- [ ] 单元测试覆盖 schema 边界、状态转换、来源引用和幂等；API 测试覆盖全部公开端点和认证/越权。
- [ ] 端到端测试至少覆盖一次完整闭环，以及上传失败、AI 超时、删除素材、令牌过期、重复请求和回复失败。
- [x] [草稿 PR #1](https://github.com/KamiZackJ/warm-letter-ai-family/pull/1) 的历史 HEAD `71d6aae` 已在 GitHub Actions 干净环境完成两条 `verify`，结果均为通过：[run 31934597929](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/31934597929) 与 [run 31934599732](https://github.com/KamiZackJ/warm-letter-ai-family/actions/runs/31934599732)。
- [ ] 提交候选版本仍需固定 Git SHA、构建产物版本、部署时间和最终证据索引；后续提交必须重新以 PR 最新 HEAD 的 CI 结果为准。
- [ ] README 与当前源码一致，包含快速运行、架构入口、演示方式、隐私边界和比赛方向。
- [ ] 匿名产品事件字典明确触发点、去重键、分母、时区、必需/禁止字段；事件不含素材、正文、签名 URL，并可对账激活、阅读、回复、第二封、成本和删除 SLA。

## 9. 视频、PDF、发布与冻结

- [ ] 视频导出时长在 30 秒至 3 分钟内，画面/字幕/声音清晰，前 15 秒可理解真实痛点。
- [ ] 视频明确展示素材输入、真实 AI 处理、来源引用、用户编辑确认、长辈阅读/回复及多形态输出。
- [ ] 视频或简介标注所有 AI 生成画面/音频/文案工具来源；快手创作声明已勾选“含 AI 生成内容”。
- [ ] 快手公开视频同时包含 `#AI未来挑战赛` 和 `#AI产品实现`，发布后无痕访问成功。
- [ ] PDF 含产品概述、目标用户、核心功能、AI 角色与价值、技术实现、应用场景，且逐页检查无乱码、溢出、低清图片或失效二维码。
- [ ] PDF、视频、Demo、代码仓库和报名字段中的作品名、团队名、功能范围和数据一致。
- [ ] 快手昵称、快手 ID、视频链接、PDF、辅助材料和团队信息已由两名成员在报名预览中交叉检查。
- [ ] 提交成功回执、页面截图、视频/PDF 校验值、Demo URL、代码 SHA 和发布时间已归档。
- [ ] 从提交到全国决赛结果公布，公开作品和已提交材料保持可访问且不删除、不隐藏、不修改；定期检查有记录。

## 10. 评委展示与答辩验收

- [ ] 3 分钟内能回答：解决谁的什么问题、为什么需要 AI、与普通 AI 写作有何不同、当前完成度和下一步商业/公益价值。
- [ ] 准备 5 分钟和 8 分钟两个展示版本，现场断网时有离线演示视频、脱敏样例和架构图。
- [ ] 能解释来源引用如何减少编造、用户确认如何保护隐私、为什么不直接读取微信聊天和完整相册。
- [ ] 能说明真实 AI 供应商、输入输出结构、失败恢复、成本估算和替换供应商的边界，不泄露密钥或内部敏感信息。
- [ ] 能展示至少一组年轻用户和一组长辈测试证据，以及由测试导致的具体改动。
- [ ] 现场展示内容与已提交冻结版本一致；新增路线图明确标注为未来计划，不冒充已实现能力。

## 11. 当前验收结论

截至 2026-08-28：当前工作树的本地 355 项回归、构建和 production bundle verifier 均通过；`case_001` 已进入脱敏固定回归，并另外完成队友真实素材的受控展示包、段落级来源映射和本地阅读/回复演示。微信双真机、真实 OpenAI、任意确认稿长图/短片、正式审核、完整删除、真人测试和生产基础设施仍未通过。因此当前仍只达到 G0 队内开发演示，不达到 G1、G2、MVP、公测或比赛提交候选。
